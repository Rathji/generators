// ─────────────────────────────────────────────────────────────
//  GOLF — Grandpa Beck's Golf, as a card game.
//  Six cards each, in two rows of three. Peek at any two, then
//  draw and swap to shape the lowest score you can: Aces 1,
//  2–10 face value, Jacks & Queens 10, Kings 0 — and a matching
//  pair in a column wipes that column out entirely.
//  Knock when all six of your cards are face up — everyone else
//  gets one final turn, then the hole is scored. Nine holes,
//  lowest total wins the tournament.
//  Modes: hotseat · vs AI · online (rooms with hidden layouts,
//  served by the server-plugin below in index.html).
// ─────────────────────────────────────────────────────────────

$meta
  title = Golf
  description = Grandpa Beck's Golf — the low-score card game. Six cards in two rows of three; peek at two, then draw and swap to build the lowest score. Pairs cancel a column, Kings are zero, knock ends the hole. Nine holes, lowest total wins. Hotseat, AI and online rooms.
  tags = card game, golf, low score, family, multiplayer, online, knock, peek
  image = https://user.uploads.dev/file/e7effd5a2f556a584f5dc93a299dc524.jpg

hubUrl = https://perchance.org/bgn-boardgame-network  // ← SET THIS to your network hub's URL
gameTitle = Golf
gameTagline = Peek at two cards, swap the rest, and knock when your six cards are in their best shape — pairs cancel a column, Kings are zero, and the lowest score wins the tournament.
gameKicker = Peek · Swap · Knock · Score
gameGenre = Card Game
gamePlayers = 2–4 players
accentColor = #3f9d6b
heroImage = https://user.uploads.dev/file/e7effd5a2f556a584f5dc93a299dc524.jpg

cardDeck = {import:card-deck-plugin}

createServerSocket = {import:server-plugin}
