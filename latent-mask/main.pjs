// Latent Mask Tool
// Paint a mask over an image, describe what should appear there, and regenerate
// just that region via image-to-image + compositing.

generateImage = {import:text-to-image-plugin}
superFetch = {import:super-fetch-plugin}
