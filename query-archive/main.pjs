// Perchance Archive Search
// Full-text search across the ~111k generator community archive dump.
// All app logic lives in src/app.js (archive URL constants are at the top of that file).

kv = {import:kv-plugin}

$meta
  title = Perchance Archive Search
  description = Full-text search across 110k+ perchance generators — names, titles, tags and descriptions — with one-click source-code inspection. Type anything: gacha, rng, pity, idle clicker, pixel dungeon…
  tags = search, archive, directory, generators, discover, index
