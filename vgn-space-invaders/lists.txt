// Space Invaders — the classic 1978 arcade game, built on the
// VGN CRT Cabinet template. The game itself lives in index.html.
// The only import needed is the server-plugin, which powers the
// online global high-score leaderboard (top-10 + players online).
createServerSocket = {import:server-plugin}
