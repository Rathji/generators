// Answer for: https://www.reddit.com/r/perchance/comments/bhm9el/if_first_result_is_a_how_to_exclude_b_from_second/

output
	The two characters are [consChar = character.consumableList, ""][c1 = consChar.selectOne] and [c2 = consChar.selectOne]. [c1] is wearing [clothing].

character
	James ^[c1 != "Anita"] // <-- read about "dynamic odds" on the perchance.org/examples page to understand this
	Khalid
	Anita ^[c1 != "James"] // this basically means "the odds of selecting this item are ^1 if c1 is not equal to 'James', and ^0 otherwise" but again, read that examples page to properly understand it
	Katie
	Fletcher
	Jade
	
clothing
	a jacket and pants
	a singlet and shorts
	a pair of overalls
	
