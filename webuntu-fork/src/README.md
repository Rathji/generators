# Webuntu OS

A browser desktop-OS simulation on Perchance — a fictional Debian-based Linux fork ("Webuntu 12, *Perch Mint*"), styled entirely with the **Rathji-template color scheme** (violet `#7c6cff`, cyan `#22d3ee`, purple `#8b5cf6` on dark navy `#0a0e17`).

## Status

- **Project complete.** All 76 roadmap tasks are implemented and validated: Tasks 1–52 (the original atomic-roadmap) plus **twenty-four** post-52 polish items (Tasks 53–76). The roadmap checklist lived in `main.pjs` during development and has been removed on close-out — the full task history now lives in `src/ROADMAP.md`.
- **Phase 10 (Coding & AI tranche, complete):** Tasks 77–88 (Webuntu Assistant, voice everywhere, AI Images, GitHub, Upload & Share, Projects, Terminal AI verbs, AI clipboard, Dev dashboard, File Manager AI, Task Manager, and **Editor → Perch Edit**) — see `src/ROADMAP.md` and the Phase 10 checklist in `main.pjs`. **All twelve tasks are done.** Task 78 was re-scoped: instead of replacing the in-OS Text Editor with a perch-edit embed, it landed as a first-class **Perch Edit** app (the real perch-edit IDE embedded in a Browser-style window) once the `perch-edit-plugin` wrapper was evaluated and found redundant.
- Latest additions: **Task 78 (Perch Edit app)** — and **Task 88 (System Task Manager)** — and **Task 87 (File Manager AI)** — and **Task 86 (Dev dashboard)** — and **Task 85 (AI clipboard actions)** — and **Task 84 (Terminal AI verbs)** — and **Task 83 (Projects)** — and **Task 82 (Upload & Share)** — and **Task 81 (GitHub app)** — and **Task 80 (AI Images app)** — and **Task 79 (Voice everywhere)** — and **Task 77 (Webuntu Assistant)** — see the dated entries below.
- **Bugfix (Browser embed loading):** the Browser app's "Loading generator…" overlay had no `load` handler and no failure fallback (and its `display:flex` beat the `hidden` attribute), so it covered the embedded generator forever — every generator opened in the OS Browser appeared to never load. `src/browser.js` now dismisses the overlay on the iframe's `load` (ignoring the `about:blank` reset) and arms a 12s slow-load timer that swaps in an "Open in new tab ↗" fallback (mirroring `src/gamelauncher.js`); `.br-loading` also gained `[hidden]`/spinner/actions/slow styles. Fixes the Browser app, the BGN/VGN hub shortcuts (`Browser.navigate`), and Software Center's "Open inside the OS browser".

- **Shortcut swap (post close-out):** VS Code Web (`vs-code-web`) removed from the Software Center catalog and the Browser start page; **Perch Edit** (`perch-edit`, a Perchance code-editor generator, verified live) added in its place — same two surfaces. See `main.pjs` (`swcApps`) and `src/browser.js` (`START_LINKS`).

- **Task 81 (GitHub app, 🐙)** — a first-class GitHub client in the Start menu (**Developer** category, AppContent builder `github`, `src/github.js`, singleton), backed by `github-data-plugin` (imported as `gh` in `main.pjs`) for repo info / file text / directory listings plus the plain GitHub REST API (`api.github.com`, CORS-open) for search, trending and issues — unauthenticated rate-limit budget respected via a 5-min in-memory cache and a 6-hour `localStorage` cache (`webuntu.gh.trend.*`) for the two search endpoints. **Home view:** 🐙 hero + "Try searching for" suggestion chips + a **🔥 Trending this week** preview (5 cards, "See more →"). **Search:** 20 repos sorted by stars, result count, click any card to open it. **Trending:** Today / This week / This month period chips. **Repo detail:** avatar, name (fork badge), description, homepage link, **Open on GitHub ↗** (routes through `Browser.navigate`, which now opens non-Perchance https URLs in a real tab — the embed browser only renders Perchance generators), meta chips (stars/forks/open issues/language/license/pushed time), topics, creation date; tabs for **README / Issues / Code**. **Issues:** number, title, author avatar + login, comment count, relative time, colored label chips (GitHub hex colors). **Code:** breadcrumb drill-down through folders, per-extension icons, file sizes, a syntax-highlight-free text viewer with a 200 KB cap and raw rendering for image files. **README:** a dependency-free markdown-lite renderer (escapes everything first, then decodes a safe entity whitelist — GitHub's `&mdash;`-style entities render properly), headings h2–h5, bold/italic/inline code, fenced code blocks, blockquotes, hr, ordered/unordered lists, tables, inline + reference-style links, and badges as images that **remove themselves if they fail to load** (shields.io hung in the sandbox — the 12s failsafe + `onerror` keep the layout clean). A 15s `AbortSignal` timeout turns hung API requests into a visible error instead of an eternal spinner, and the rate-limit-aware error panel offers retry. Verified live: search → open `mrdoob/three.js` (stars/forks/379 issues, README with 5 badge images + code blocks + links, 8 issue rows, 22-root code listing, `src/` drill-down, file view), trending period switch, back-navigation (repo → list → home via the Search tab), singleton relaunch, deep-link capture, plus a vision pass on the README and both **desktop (1920×1080)** and **phone (390px, no horizontal overflow, chips/cards wrap cleanly)** layouts — clean console.

- **Task 88 (System Task Manager, 🗂️)** — **Ctrl+Shift+Esc** (documented in the help dialog) or Start menu → System opens a windowed process manager (`src/taskmanager.js`, `window.TaskManager`, AppContent builder `task-manager`, singleton). It lists **every open window** across all desktops as rows with icon + title, a **stable PID** (1000 + window id), live **CPU% / Memory bars** sharing the System Monitor's exact per-window random-walk model (`SystemMonitor.procStats` was exported for this — the two apps always agree), a **Desktop chip** (cyan when it's the current desktop), and a **state chip** (Running / Focused / Minimized / Maximized / Snapped, each color-coded). Columns are **sortable** (Process / PID / CPU / Memory / Desktop / State — click toggles asc/desc, arrow glyphs, play an `ok` blip), and a **filter box** narrows rows by title or app id. Row actions: **🔀 Switch to** (restores if minimized, focuses it, and **switches desktops** if it lives elsewhere via `Workspaces.switchTo`), **▁ Minimize / ▢ Restore**, **⛶ Maximize / Restore size**, and **✕ End process…** — a GNOME-style **two-step confirm** (\"End \"title\"? Its window will close and unsaved changes will be lost.\") that runs a new **`WM.forceClose`** (like `close` but bypassing the app's `onCloseRequest` hook, so even an unsaved Text Editor is killed), then a toast + `close` blip. Keyboard: **↑/↓/Home/End** move the selection, **Enter** = Switch to, **Delete/Backspace** = End process; rows support click + double-click-to-switch. A footer shows the live totals (process count, CPU %, memory used of 8 GB — from `SystemMonitor.snapshot()`). The 1.5s refresh loop **self-stops when the window closes** (detached-DOM guard + `onCloseRequest`). Verified live: opens via Ctrl+Shift+Esc and the System menu, sorts on every column, filter narrows rows, Switch to wakes a minimized window, End process (two-step) force-killed a Text Editor holding unsaved changes, the Task Manager's own row can end it, stats agree with the System Monitor app, footer totals tick, light theme + phone (390px) layout fit, console clean. Also marked ROADMAP Tasks 41–50 `[x]` (they were implemented long ago but the markers had gone stale). Final polish: a `≤560px` media query **hides the Desktop column** and slims PID/gauges/State so process names stay readable on phones (verified: all four names render untruncated, no body/document overflow), and the gauges got an explicit `flex:0 0 117px` basis — without it the nested-flex gauge box collapsed (~30px) and its shrink-0 track/label overflowed into the Desktop column, overlapping the chips (pixel-measured, then fixed and re-verified in both themes).

- **Task 78 (Perch Edit app, ⌨️)** — the re-scoped Editor task lands as a first-class Start-menu app (**Developer** category, AppContent builder `perch-edit`, `src/perchedit.js`, `window.PerchEdit`, singleton) that embeds the real `perchance.org/perch-edit` IDE in a Browser-style window: `<iframe src="https://null.perchance.org/perch-edit">` (the platform auto-redirects to the generator's subdomain — the same embed the Browser app / Game Launcher use), a Rathji-themed toolbar (**⟳ Reload**, **↗ Open in new tab**, plus a "projects save in your browser" chip), a loading overlay with a **12s slow-load fallback** offering "Open in new tab" when the embed stalls, a footer note that PerchEdit keeps its own project tree in the browser (so virtual-FS files still belong to the native Text Editor), and an **on-close iframe blank** so embedded work/audio stops. Terminal `open perch-edit` routes here via the app catalog (`Apps.getById`). The **`perch-edit-plugin`** wrapper (`perchance.org/perch-edit-plugin`) was evaluated live first — its API (`embed`/`card`/`all`/`find`/`data`/`runCode`) imports cleanly and `embed()` does produce a working embed (redirect + cross-origin confirmed, no SecurityError), but it was **found redundant**: it emits exactly the same `null.perchance.org` iframe Webuntu already uses everywhere, its catalog's four slugs all point at the same URL, `runCode` is a bare `eval`, and it drags in four plugin deps (kv/ai-text/upload/super-fetch) — so the app embeds the generator directly instead of importing the plugin. It also cannot deliver the original 78's ".txt/code files open there": PerchEdit's virtual file tree lives in its own IndexedDB origin, unreachable from Webuntu's virtual FS. Verified live: Start-menu launch, `open perch-edit` in the Terminal, singleton refocus, embed redirects to the real subdomain, reload, on-close blanking, light theme + phone (390px) layout, clean console.

- **Task 87 (File Manager AI, 🧠)** — right-clicking a folder in File Manager now offers three AI actions: **📝 Summarize contents**, **📄 Generate README**, and **🧱 Suggest file structure**. Each builds a bounded textual inventory of the folder tree (≤200 items, real sizes, plus 140-char previews of small text files), feeds it to `generateText`, and **streams** the answer into a modal dialog (scrollable monospace `<pre>`, pulsing ✨ Generating status, **📋 Copy** → OS clipboard history, Close/backdrop/Esc cancels an in-flight call via `.stop()`). While running, the FM status bar swaps to a pulsing "✨ …" (`fm.busy`, `.busy` class; menu items disable). **Summarize** just displays; **Generate README** writes `README.md` and **Suggest structure** writes `SUGGESTED-STRUCTURE.md` straight into the folder (overwrite-aware, exact byte sizes, listing auto-refreshes). All three verified live on `/home/user/Projects` — Summarize produced an accurate 6-bullet description, README.md and SUGGESTED-STRUCTURE.md landed in the folder with real content, Esc-stop cancelled cleanly, Copy landed in clipboard history, file/Trash menus correctly omit the AI block, phone (390px) layout fits. Also fixed a latent platform leak: `.fm-preview-text` now forces `text-align:left` — the platform's default `body{text-align:center}` was silently centering every line of long-form AI output.

- **Task 86 (Dev dashboard, 🛠️)** — the Control Center grew a **Developer** section (Settings sidebar, between Notifications and Users; `Settings.openSection("developer")`, new `set-dev-*` CSS) — a one-glance status dashboard. **Status tiles:** 📅 today's date, 🌐 live online-user count (from `src/net.js`), 📤 session upload quota (`window.Uploads.quota` — file count + bytes), 🪟 open-window count. **Today's tasks:** one row per project (via `src/projects.js`) with to-do/doing counts (hot-highlighted when there's work), plus a **"Next:"** line proposing the single next task with a one-click **✓ Done** button that advances the queue live. **Weather:** reuses the Weather app's cached forecast (`webuntu.weather.cache`) with an "Open Weather" shortcut. **System activity:** a live line graph fed by `SystemMonitor.snapshot()` (cyan = CPU, purple = RAM over the shared fictional model, faint gridlines, meta line CPU/RAM/uptime/windows) — pre-seeded with ~2 minutes of history so it's alive instantly; its 2s ticker is torn down when Settings closes. **Quick AI:** three prompt chips (🎯 Today's focus — prefilled with the open project tasks, 💡 Brainstorm, ✍️ Draft email), an "Ask anything…" field, and a **Run/Stop** button that streams `generateText` output into a monospace pre. Verified live: all four tiles populate, ✓ Done advances the queue, weather shows the cached Tokyo forecast, graph draws full-width cyan+purple lines (pixel-verified), Quick AI streams a drafted email with Run→Stop→Run cycle, console clean, desktop grid + phone (390px) layout with no overflow.

- **Task 85 (AI clipboard actions, ✨)** — selected text anywhere in the OS can be AI-transformed. **Super+V picker:** every entry gained a **✨ button** that pops a small **Polish / Rewrite / Summarize** menu (hover-highlighted; the **`A`** key opens it for the highlighted row — added to the footer hint). While generating, the row shows a pulsing **⏳** and the picker stays open; the result then **replaces the selection** at the field that was focused when the picker opened, or lands on the real clipboard with a toast when none is focused. **Esc** cancels a running generation. **Right-click edit menu:** right-clicking inside any textarea/input/contenteditable now shows the OS menu instead of the browser's — **Cut / Copy / Paste** (disabled appropriately; paste falls back to `clipboard.readText` + programmatic insert when `execCommand` is blocked) plus **✨ Polish / 🔁 Rewrite / 📝 Summarize**, with a small pulsing **"✨ Polishing…"** chip pinned to the field while the AI runs. A shared `ClipboardHistory.aiAction(kind, text)` helper (also exported) wraps `generateText` with a per-action instruction (Polish fixes grammar/spelling/wording; Rewrite re-expresses the same meaning; Summarize condenses to key points), and every result is added back into clipboard history. Verified live end-to-end: picker ✨ → Summarize replaced a textarea selection, right-click → Polish corrected a typo'd sentence in place, no-selection disables the Cut/Copy/AI items, `A` shortcut opens the menu, busy chip cleans up; console clean.

- **Task 84 (Terminal AI verbs, ⌨️)** — the Perch shell gained three AI commands reusing the Phase 10 imports. **`ai "<prompt>"`** calls `root.generateText` with `onChunk`, streaming the answer straight into a terminal line that starts as a pulsing \"ai: thinking…\" (`term-busy` animation) and grows as tokens land — the shell stays usable while it runs (async commands were already supported since Task 82's `upload`). **`gh search <query>`** hits `api.github.com/search/repositories` (sorted by stars, 8 results, 5-min in-memory cache, 15s abort timeout) and prints `⭐ stars  owner/name [Language] — description` lines; GitHub rate-limit messages surface cleanly. **`img "<prompt>"`** runs `root.generateImage(prompt, {resolution:"768x768"})` and saves the result as a unique `.png` in `/home/user/Pictures` (folder auto-created if missing, slugified-prompt + timestamp name, name collision loop), then prints the saved path plus a hint to open or `upload` it. HELP and `man` list all three. Verified live: `gh search perchance` returned 8 real repos, `ai` streamed a 5-word reply, `img` saved a 63 KB image into Pictures; no console errors.

- **Task 83 (Projects, 🗂️)** — a local-first project manager in the Start menu (**Developer** category, AppContent builder `projects`, `src/projects.js`, singleton). New `window.Projects` data layer persisted in the `kv-plugin` (folder `webuntuProjects`, keys `list`/`tasks:<id>`/`active`, with an in-memory fallback if kv is unavailable) — `createProject`, `renameProject`, `deleteProject`, `setActive`, `addTask`, `moveTask`, `setStatus`, `setPriority`, `deleteTask`, `bumpPomodoro`, `nextTask`, `saveBacklog`, `importBacklog`. **The app:** a sidebar of projects (＋ new, ✎ rename, 🗑 delete via custom in-app modals — no native dialogs), a 3-column kanban (📋 To do / 🔥 Doing / ✅ Done) with per-column add rows, priority pickers (🔵/🟡/⚡), 🍅 pomodoro counts and ◀ ▶ ✕ card controls, and a 🤖 suggestion strip proposing the **single next task** (the task in progress, else the highest-priority oldest open one) with **✓ Done** and **▶ Focus 25** buttons. **Pomodoro:** 25 min focus → 5 min break (15 min every 4th cycle), auto-advancing phases, a ticking progress bar, and **+1 🍅 on the task in progress** each time a focus phase completes. **Backlog sync:** each project exports/imports a GitHub-style checklist (`- [ ]`/`[~]`/`[x]` + priority + 🍅 tags) at `/home/user/Projects/<slug>.md` on the virtual file system (folder auto-created), import dedupes by task title (bug caught live: the priority emoji wasn't stripped before the title match, so re-import duplicated every task — fixed by cleaning the title first). **Assistant:** two new tools — `tasks|<project>` lists a project's open tasks and `nexttask` proposes the one to work on, wired into `execAction` and the system prompt. First run seeds a "🚀 Getting Started" project. Verified live end-to-end: seeding, add/move/delete tasks, rename/delete projects, pomodoro ticking + skip→break→focus, backlog write/read (real `.md` on the FS), nextTask, phone (390px) layout with a `ResizeObserver`-driven `.narrow` class so small windows on desktop stack like a phone does; a scoping bug (module-level timer functions calling `build()`-local renderers → masked "Script error") was found and fixed by moving the whole pomodoro timer into `build()`. Known benign quirk: `snapshot.js` full-page captures hang while the preview iframe is display-scaled (phone viewport), so phone checks used DOM metrics instead.

- **Task 82 (Upload & Share, 📤)** — the OS can host files and text on Perchance's storage. New `window.Uploads` helper (`src/uploads.js`): `upload(string|Blob|data-URL)` wraps `upload-plugin` (imported as `uploadPlugin` in `main.pjs`), converts data-URLs to Blobs so quotas are billed correctly, records every success in a session history (`webuntu-uploads` event), and **shares each URL** — into the Super+V clipboard history AND the real clipboard, so "upload, then Ctrl+V anywhere" just works. Wired into three surfaces: **(1) File Manager** right-click menu gained **📤 Upload & get link** (uploads the file node, disabled for folders/multi-select/Trash), **(2) the Terminal** gained an async `upload <path>` command (the executor now supports Promise-returning commands), and **(3) a new **Easy Uploads** app (Start menu → Internet, 📤, singleton) with **✏️ Text / 📎 File** tabs — text mode has a live byte counter and an "Upload" button with busy state, file mode accepts drag-and-drop or click-to-browse (multiple files, removable chips), and both end in a result card with **Copy** + **↗ Open** (routes through the OS Browser) plus a "This session" recent list. Verified end-to-end live: text upload, file-mode upload (real user.uploads.dev URLs returned, chips cleared, recent list updated), FM right-click upload, Terminal `upload`, and a phone (390px) layout pass — no overflow, windows auto-fit the viewport. Debugging note: the File tab appeared dead because the tab handlers passed booleans (`setMode(true)`) while `setMode` compared against the string `"file"` — both tabs silently switched to text mode; fixed by passing `"text"`/`"file"`.

- **Task 80 (AI Images app, 🎨)** — a proper image-generation studio in the Start menu (**AI** category, AppContent builder `ai-images`, `src/aiimages.js`). It reuses the `generateImage` (`text-to-image-plugin`) import already added for Task 77, so no new plugin. Layout: a **control panel** (prompt textarea with a Ctrl/Cmd+Enter shortcut, optional negative-prompt field, a resolution picker — square/portrait/landscape/large 768×768 — and a gradient **✨ Generate** button), a **stage** (checkerboard backdrop; while generating it shows a spinner + pulsing \"Generating…\" over a dimmed overlay), and an action bar beneath: **💾 Save to Pictures** (writes a `data:image` file into `/home/user/Pictures` with the same FS.create recipe the Assistant uses — deduped names, correct byte size, so Image Viewer can open it), **🖼️ Set as wallpaper** (`Desktop.setWallpaper`, persists), **🔍 View** (opens the result in the Image Viewer app) and **🔀 Reroll** (re-runs the current prompt with a fresh random seed; the prompt field is pre-filled so edits are easy). A **session history strip** at the bottom keeps the last 12 images as clickable thumbnails (in-memory by design — data-URLs are too big to persist; images the user wants to keep go to Pictures). The window is a **singleton**; generation is cancellable (closing the window abandons the in-flight call cleanly). Errors and \"no prompt\" get toasts; generations have a visible animated loading state throughout. Verified: launch from Start menu, empty-prompt guard, a real generation (dataUrl returned), Save-to-Pictures (file lands in the virtual FS with correct size), wallpaper set, Reroll with a changed seed, history thumbnails + click-to-restore, window-close mid-generate leaves no errors, desktop + phone (390px, single-column stack) layouts, clean console.

- **Task 79 (Voice everywhere)** — the OS can now hear and speak, powered by `voice-tools-plugin` (imported as `voiceTools` in `main.pjs`). **OS-wide dictation (Win+H style):** a new tray **🎤 mic** (`#trayMic`) and **Super+H** (documented in the help dialog) start/stop dictation into **any focused text field** — `src/voice.js` (`window.Dictation`) captures the focused `input`/`textarea`/`contenteditable` and its caret, then streams speech-to-text (Chrome/Edge/Safari Web Speech) through `voiceTools.listen`, **inserting each final utterance at the caret** with the emoji.js insert technique (execCommand + input-firing fallback, so editors' dirty/undo tracking works). While listening, a **floating bar** (`#dictBar`, top-center) shows the live transcript and the tray mic **pulses**; **Esc** or a second mic press stops. With **no editable field focused**, the dictation text is copied to the real clipboard on stop (with a toast). Unsupported browsers (no SpeechRecognition) get a friendly toast instead. The mic is locked-inert and closes tray popups / the Start menu on start. **Assistant TTS:** the Webuntu Assistant gained a **🔊/🔇 speaker toggle** in its header (`webuntu.assistant.speak`, persisted, on by default) — each reply (including action follow-ups) is **read aloud** via `voiceTools` with markdown-lite tokens stripped first (no "star star bold"), queued so consecutive replies don't interleave; sending a message, starting a New chat, or closing the window **stops** the speech. A new **🗣️ AI Voice Assistant** app (a voice-managed TODO-list generator, already in the Software Center) now also appears in the **Start menu** (AI category, link-type). Verified: import + API reachable (via the plugin's cached `window.__voiceToolsPluginApi`), TTS fires during real replies (isSpeaking sampled mid-turn), speak toggle + persistence, Super+H + mic click routes (unsupported-browser toast), Start-menu tile, tray fits desktop + phone (390px) widths, clean console.

- **Task 77 (Webuntu Assistant, Super+A)** — the flagship "AI OS" feature: a first-class chat app (`src/assistant.js`, `window.Assistant`, AppContent builder `assistant`) that can *act on the OS*. Imported `ai-text-plugin` (`generateText`) and `text-to-image-plugin` (`generateImage`) into `main.pjs`. Opens via **Super+A**, the Start menu's new 🧠 **AI** category/tile, or its taskbar pin (singleton — Super+A toggles minimize/restore). Chat UI: welcome bubble, suggestion chips (horizontally scrollable on phones), streaming typewriter replies with a visible spinner, and markdown-lite rendering (headers/bold/italic/code/inline-code/lists — fixed so multi-child output actually renders). The model answers via a `[[ACTION]]tool|arg[[/ACTION]]` protocol, producing grounded **result chips**: `launch` (opens any catalog app via `Apps.launch`), `read` (reads a virtual-FS text file), `list` (lists a folder), `write` (creates/overwrites a file, e.g. `/home/user/Documents/...`), `search` (installed apps + Software Center catalog + categories), `img` (generates an image with Save-to-Pictures + Set-as-wallpaper buttons), `status` (OS info). Replies are two-phase: **Phase A** runs the tools and shows the chips, **Phase B** follows up in natural language grounded on the tool results (phase-budgeted compaction keeps it from exploding). The session persists across reloads (`webuntu.assistant.session`); **New chat** resets it. Documented in the help dialog. Verified: every tool, streaming, chips, Phase-B grounding, persistence across reloads, welcome formatting, Start-menu AI category, Super+A open/toggle-minimize, phone layout (window spans the 390px viewport exactly; no horizontal page overflow), clean console.

- **Task 76 (Clipboard history, Super+V)** — every text copy/cut is recorded in a persisted history (`src/cliphistory.js`, `window.ClipboardHistory`, `webuntu.clipboard.history`): copy/cut events are intercepted globally **and** `navigator.clipboard.writeText` is wrapped so programmatic copies (emoji picker, terminal, copy buttons) land in it too; re-copying the same text just bumps it. **Super+V** opens a Windows-style picker anchored near the focused field — ↑/↓/Home/End move the selection, **Enter (or click) pastes at the caret** of the field focused on open (emoji.js's insert technique), Delete/Backspace removes an entry, Esc/outside-click/resize close. Entries can be **pinned** (📌, survive Clear all) or hover-deleted; with no field focused a paste copies to the real clipboard + toast. While open the picker owns the keyboard (shortcuts stand aside, no Start-menu pop on release); the lock screen blocks it; it mutually closes with the Start menu/tray/emoji picker/help/run dialog. Documented in the help dialog. Verified: capture (copy/cut/writeText), dedupe, persist, paste-at-caret, pin/sections/clear-all, delete, mutual exclusion, locked-inert, phone layout, clean console.

- **Task 75 (Show Desktop, Super+D)** — **Super+D** (or the new **far-right tray peek-strip button** `#trayDesktop`, a slim monitor glyph at the taskbar's edge) minimizes **every window on the current desktop**; a second press **restores exactly the ones that were visible**. `WM.toggleShowDesktop`/`WM.isShowDesktopActive` (src/wm.js) **re-snapshot the visible set on each activation**, so the toggle stays self-consistent through opens/closes — a window moved to another desktop is skipped on restore, one closed while hidden stays closed, and maximized/snapped windows come back in the same state. Focus clears while active and lands on the **top restored window**. The button's `.active` highlight is polled with the pin dots so a Super+D press keeps it in sync; both triggers are locked-inert. Documented in the help dialog. Verified: Super+D hide/restore (flags + focus), button toggle + `.active`, maximized restore, empty desktop no-op, locked-inert, taskbar-top + phone layouts, clean console.

- **Task 74 (Window overview / Mission Control)** — **Super+Tab** opens a full-screen overview of every window on the *current* desktop (`src/overview.js`, `window.Overview`) as a **grid of live-preview tiles**: each tile is an inert scaled clone of the real window plus icon/title and a per-tile ✕. Clicking a tile (or Enter on the selected one) **focuses/restores** that window and closes the overview; Esc/backdrop dismiss; Backspace/Delete closes the selected window; arrows move the selection (left/right wrap, Home/End jump). Minimized windows render dimmed and restore on click; windows on other desktops are skipped. While open, the overview owns the keyboard (global shortcuts stand aside); Super+Tab opens it and steps the selection while open; releasing Super never pops the Start menu; the lock screen blocks it; an empty desktop shows a "No windows are open" toast. Documented in the help dialog. Verified: open/step/Enter/Esc/backdrop/✕-close (grid rebuilds), minimized-dim + restore, arrow wrap, cross-desktop exclusion, no-windows toast, locked-inert, Start-menu-no-pop, clean console.

- **Task 73 (Drag-to-edge snapping)** — dragging a window to a screen edge shows a translucent accent **snap-preview** (left/right half, or full width at the top) that follows the cursor and **snaps on release** (`WM.snap`/`WM.maximize`). Dragging a **maximized** window's title bar un-sticks it after a small pull — restoring the pre-max rect *under the cursor* and continuing as a normal drag; a grab-click that never pulls leaves it maximized. The Task-71 un-snap-on-drag behavior is preserved, and the preview hides the moment the cursor leaves every zone. Verified: all three zones' preview rects, release→snap/maximize, mid-drag zone switching, maximize→pull-down→drop, grab-click stays maximized, no console errors.

- **Task 72 (Start menu universal search)** — the search box now searches the whole OS, not just apps. While a query is set, the app grid is replaced by **grouped result rows** (with type chips): **Applications** (id/name/blurb → `Apps.launch`), **Files** (virtual-FS walk via the Run dialog's `Shortcuts.fsNodes()`, opened with `Shortcuts.openNode` like a File Manager double-click), **Settings** (jumps to the section via `Settings.openSection`) and **Commands** (help + power actions). Rows are keyboard-walkable (arrows + Enter), clicking runs the row and closes the menu, unknown queries show a "Nothing found" state, and an empty query still shows the normal app grid + recents. `src/shortcuts.js` now exports `fsNodes()`/`commands()`/`openNode` for reuse. Verified: "doc" → Applications+Files, "sound" → Settings, "lock" → Commands, file/settings/app row clicks all act correctly, arrow nav, no regressions, no console errors.

- **Task 71 (Window snapping)** — **Super+←/→** snap the focused window to the left/right half of the work area, **Super+↑** maximizes it, **Super+↓** restores (un-maximize / un-snap) or minimizes. Implemented in `src/wm.js` as `WM.snap(id, "left"|"right")` / `WM.unsnap(id)` (dispatched in `src/shortcuts.js`), reusing maximize's `_prevRect` machinery so a maximize→snap→restore chain returns the window to its exact original spot; the model keeps its real geometry while snapped and `sync()` renders the half via the `snapped-left`/`snapped-right` classes (outer corner stays rounded, shared inner edge squares up, resize handles hidden). Dragging or resizing a snapped window un-snaps it first (Windows-style); snapping wakes minimized windows; documented in the help dialog. Verified: exact-half rects for both sides, ↑ maximize, ↓ restore chains, un-snap on drag, wakes minimized windows, no Start-menu pop on release, works with a top taskbar, no console errors.

- **Task 70 (Run-a-command dialog)** — **Alt+F2** (help dialog documents it) opens a GNOME-style **Run dialog** (`#runDialog`, wired in `src/shortcuts.js`). Live suggestions grouped by kind: **apps** (catalog id/name/blurb match → `Apps.launch`), **file/folder paths** in the virtual FS (opened like a File Manager double-click — folders, shortcuts, PDFs/images/text), **curated commands** (`help`, `lock`, `restart`, `shutdown`, `suspend`, `switch-user` → `PowerMenu.act`), and **`terminal -c "<cmd>"`** (runs in the Perch shell via a new `window.Terminal.run` hook that opens the terminal, waits for the live instance and feeds it the command). **Enter** runs the highlighted suggestion or typed text, **Tab** fills the input, **↑/↓** walk suggestions or the persisted history (`webuntu.run.history`, capped 12), **Esc**/backdrop click close. Unknown input → "Command not found" toast. Verified: Alt+F2 open, app/path/command suggestions, folder/file/shortcut opening, `terminal -c` passthrough, Enter/Tab/arrows, history recall + persistence, Esc close, inert while locked.

- **Task 69 (Display settings)** — Control Center grew a **Display** section (🌓) and the tray volume popup became a mini quick-settings, both driving a new `src/display.js` (`window.Display`). **Brightness**: a full-screen black overlay (`#dimOverlay`, `pointer-events:none`, z 9900 — below only the boot screen) whose opacity = 1 − brightness, so it dims windows, menus and even the lock/power screens like a real panel; clamped to a 5% floor so the screen can never go fully black. **Night light**: a warm `#ff9a3c` overlay (`#nightOverlay`, `mix-blend-mode:multiply`) whose opacity follows an intensity setting — the f.lux look (whites → cream, darks warm up). Both persist in `webuntu.settings` (`brightness`/`nightLight`/`nightIntensity`), re-apply on boot + after Reset (via `Settings.applyAll`), and are exposed as `window.Display.get/setBrightness`, `get/setNightLight`, `get/setNightIntensity`, `applyAll`. The volume popup's quick controls sync from `window.Display` on every open, so the two UIs stay in step. Verified: live apply + persistence across reload, toggle + intensity, quick↔Control-Center sync, brightness floor, overlays never block clicks, Reset restores 100%/off.

- **Task 68 (System emoji picker)** — a GNOME-style picker for the whole OS (`src/emoji.js`, `window.EmojiPicker`). **Super+.** opens it anywhere (documented in the help dialog); **Text Editor** and **Notes** gained **🙂 toolbar buttons** that anchor it to the button. Search box (matches emoji/keyword/category, Enter inserts the top hit), a **Recent row** (persisted `webuntu.emoji.recent`, capped 24), **10 category tabs** and an 8-column grid with tooltips. Clicking inserts at the caret of the focused `input`/`textarea`/`contenteditable` (`execCommand` with a manual fallback that still fires `input`, so Text Editor dirty/undo work); no field focused → copies to the real OS clipboard + toast. Esc / outside-click / resize close it; opening it closes Start menu/tray popups/help/menus. **Bonus fix**: Super now toggles the Start menu on *release-only* (when no other key was used), so Super+. and Super+/ combos no longer pop the Start menu. Verified: Super+. open, categories, search, Enter-insert, caret insertion + dirty state, no-field copy toast, recents persist/reorder/cap, Esc/outside-click close, viewport clamping, no Start-menu pop during combos.

- **Task 67 (File Manager clipboard)** — a new OS-wide virtual clipboard (`src/clipboard.js`, `window.Clipboard`, in-memory, shared by every File Manager window). **Copy / Cut / Duplicate** (item context menu + Ctrl/Cmd+C/X/D) and **Paste** (Ctrl/Cmd+V, toolbar 📥 Paste button, background-menu Paste, **"Paste into \<folder\>"** on folder items). Cut items dim with a dashed outline (live-updated across windows via a `webuntu-clipboard` document event); **Esc cancels a pending cut**; the status bar shows the live clipboard ("· 📋 2 copied"). `fs.js` gained the deep-copy core: `deepSpec` / `uniqueCopyName` ("(copy)", "(copy 2)"…) / `copyInto` / `moveInto` (both refuse `/` + home + folder-into-itself; move consumes itself on success). Context menus gained separator support (`.ctx-sep`, keyboard nav skips them). Verified: multi-copy→paste, duplicate, cut→dim→move→consumed, Esc cancel, deep folder copy with content intact, Paste menus, keyboard nav, toasts.

- **Task 66 (Clock calendar popup)** — clicking the tray clock opens a **month-view calendar popup** (systembar.js + `#clockPopup`): ‹/› arrows flip months, the month label jumps back to today, out-of-month days navigate the view, today is ringed + dotted, clicking a day closes it, footer shows today's full date. It joins the tray popup family (mutual close with volume/notifications/power, Escape + outside-click, `.active` button state). Theme-token styled. **Bonus fix**: at ≤640px the tray's clock + power ran off-screen; a media query now hides the decorative online/network/battery icons + Start label and slims pager chips, so the whole taskbar fits (verified 390×844).

- **Task 65 (Terminal power-up)** — the Perch shell (`src/terminal.js`) gained a real Linux feel: shell env + `$VAR` expansion (`echo`, `env`), `echo >` / `echo >>` file redirection, new commands `cp` / `mv` (deep-clone + same-dir rename) / `head` / `tail` / `grep -i` / `wc` / `tree` / `man` / `history -c` / `uptime` / `free -h` / `env` / `sudo` (always refuses, with an `rm -rf /` gag), `ls -l`/`-lh` (fixed a bug where `ls -l` treated the flag as a path), **path-aware Tab completion** (folders get a trailing `/` and Tab drills in; `~/` and relative paths both work), **persisted command history** (`webuntu.term.history`, survives reloads), and a colored **`neofetch`** with a Rathji-palette box-drawing W logo + system info (OS/DE/Shell/Kernel/Uptime/Host/User/Display/Theme/Terminal). Verified end-to-end in the terminal, incl. history surviving a reload.

- **Task 64 (Virtual desktops)** — new `src/workspaces.js` (`window.Workspaces`) adds 1–6 desktops (default 4, count in Settings → Personalization → Workspaces, `webuntu.settings.wsCount`): a numbered **pager** in the taskbar (`#deskPager`), Ctrl+Alt+←/→, Ctrl+Alt+Home/End and Ctrl+Alt+1..N switching. Every window is pinned to the desktop active when it opened; switching hides other-desktop windows + their taskbar entries and re-focuses the top window of the new desktop. Launching a singleton app open on another desktop moves it to the current one. Optional **per-desktop wallpapers** (`webuntu.settings.wsWallpapers` overrides, applied via new non-persisting `Desktop.apply`): set from the desktop context menu's "Set this desktop's wallpaper…" (picker in desktop mode, reset clears back to global). Desktop context menu also gained flat "Switch to Desktop N" items. Verified: pager + switching, spawn-on-active-desktop, hiding (windows + taskbar), focus re-focus, keyboard combos, count control (6→3), cross-desktop singleton move, per-desktop wallpaper set/switch/reset, phone layout.

- **Task 63 (Sound themes)** — Settings → Sound gained a sound-theme picker: five WebAudio-synthesized UI-sound themes in `src/sounds.js` that each remap every recipe (open/close/error/startup/notify/ok) — **Rathji** (the original Task-27 sounds, unchanged), **Retro** (8-bit square-wave bleeps), **Ocean** (slow gentle low sines), **Mechanical** (sharp clicky ticks) and **Muted** (barely-audible whispers). Selecting a chip persists `webuntu.settings.soundTheme` and plays a three-note preview that works even with UI sounds off (only gated on the first user gesture). Theme resolution is per-`play()`, so live sounds follow the active theme instantly; unknown values fall back to default. New `Sounds` API: `themes` / `theme` / `preview(id)`, plus a new `ok` recipe for the Welcome screen. Verified: chips render/select/persist/preview, all five themes play error-free, desktop grid + phone stacking, fallback on unknown value.

- **Task 62 (User accounts)** — the lock screen grew a real multi-account system (`src/session.js`): accounts carry a display name + emoji **avatar** (16-choice picker, default = username initial), and the lock screen shows a **Linux-style account picker** (clickable avatar chips per account, last-signed-in pre-selected, plus a dashed "+ New user" chip that swaps in the create form). The **Start menu user chip** shows avatar + display name and opens a user menu (Account settings / Switch user / Lock); **Switch user** also lives in both power menus. **Settings → Users** (👥): your-account card (edit profile: display name + avatar grid; change password, re-salted/re-hashed), accounts list with Edit/Delete (delete refuses the only account; deleting yourself signs out to the lock screen), and add-account (same validation as the create form). New `window.OS` API (`createAccount`/`updateAccount`/`deleteAccount`/`accountsInfo`/`displayName`/`avatar`/`avatarChoices`/`switchUser`) is shared by the login form and Settings; `unlock()` fires a `webuntu-userchange` event so the Start menu refreshes live. Verified: create + validation errors, update (avatar/name/password), lock-screen picker, sign-in, Start-menu chip/menu, Users CRUD, delete-self → lock, phone layout.

- **Task 61 (PDF Viewer app)** — the Accessories stub became a real app (`src/pdfviewer.js`, `window.AppContent["pdf-viewer"]`, singleton, removed from the Software Center's planned list): real PDF rendering via **pdf.js v3.11.174**, loaded lazily on first open as an esm.sh main bundle + raw jsdelivr worker script (the **fake-worker main-thread** path — no cross-origin worker, wedge-proof). Opens PDFs from the File Manager (double-click `.pdf`), its own Open dialog (recursive FS scan), or drag-and-drop. Toolbar with thumbnails sidebar (click-to-jump, current-page highlight), Ctrl+F search across pages (match counts + snippets), page nav + jump, fit-width/fit-page/1:1/zoom. Selectable text layer over the canvas; status bar shows page/total/zoom; keyboard navigation (arrows/Space/PgUp/PgDn, +/-/0/F); errors surface with a retry. Seeds three sample PDFs (release notes 2p, user guide 3p, shortcuts 1p) into Documents via a tiny **in-app PDF writer** (uncompressed Helvetica, correct xref, footers) — samples regenerate if stored content diverges from the writer. Reopening a PDF switches the single instance's doc in place. Verified: open from File Manager + Open dialog + reopen-switch, 2–3 page docs render, thumbnails all render + click-to-jump, page nav, zoom, search, text-layer spans, no console errors.
- **Task 60 (Mail app)** — the Accessories stub became a real app (`src/mail.js`, singleton, removed from the Software Center's planned list): a self-contained email client for the fictional Webuntu universe. Curated address book (10 contacts with avatar emoji + color), seeded mailbox (~17 messages telling a little story), folders **Inbox/Starred/Sent/Drafts/Trash** with live counts (red unread badge on Inbox), **compose** with contact autocomplete, **reply/forward** with quoting, auto-save draft on cancel, **search** across all non-trash folders (folder tag on each result), read view with Star/Delete, per-message Restore + two-step Empty Trash in the Trash view, and "Mark all read". It feels alive: a scripted message **arrives** ~50–120s after first load (bell notification + toast + tray badge when the app is closed, click → opens the message), and sending to certain contacts can earn a delayed reply. Persists in `webuntu.mail` (versioned, re-seeds on bump). Theme-aware + responsive (sidebar → horizontal folder rail on phones). Verified: launch from Start/Software Center, folders/counts/unread, read view, compose → Sent, reply/forward quoting, draft save, search, delete → Trash → restore, empty trash confirm, persistence across reload, phone layout.
- **Task 59 (Weather app)** — the Accessories stub became a real app (`src/weather.js`, singleton, removed from the Software Center's planned list). It fetches **live forecasts from Open-Meteo** (no key, CORS-open) for 28 curated cities plus a "use my location" geolocation button, with a °C/°F toggle — both the city and the unit persist in `webuntu.settings`. UI: condition-tinted **hero** (sun/cloud/rain/snow/fog/thunder/clear-night gradients), 8 detail cards (humidity, wind + compass, pressure, UV, precipitation, cloud cover, sunrise/sunset), a 24-hour strip ("Now" highlighted), a 7-day list with gradient high/low temp bars, a stylized **precipitation radar** (rings + sweep + intensity blobs), and **derived alerts** (thunder/wind/heat/cold/rain-likely). The last forecast caches per city+unit (`webuntu.weather.cache`) so the app works offline with a stale-data banner, and Refresh recovers. Verified: real London/Tokyo data, city/unit switch + persistence, singleton, offline cache + retry, phone layout, light theme.
- **Task 58 (Notifications center)** — `src/notify.js` (`window.Notify`) adds a tray **bell** with an unseen-count badge, a popover **notification center** (app/title/body + timestamps, Clear all, empty state, click-to-dismiss + click-to-action), and **toast popups** (6s countdown progress bar, dismiss ✕, click-to-action, capped at 4, slide-in from the right; docked above the taskbar, flips to top-right when the taskbar is docked top; honors Reduce Motion). Notifications are in-session only. Two apps feed it: **Perch Chat** (messages arriving while chat isn't focused → notification whose action opens the chat) and **Minesweeper** (a cleared field → difficulty + time, action reopens the game). Settings gained a **Notifications** section (🔔) with two persisted toggles: "Show notifications" (`webuntu.settings.uiNotifications`, controls toasts only — the center + badge keep records either way) and "Notification sounds" (`notifySounds`, an independent blip via `Sounds.blip`). The bell is wired into the systembar's popup management (outside-click/Esc dismisses; opening volume/power/Start closes it). Verified: toast + badge + center + Clear all, chat-open-on-click, Settings toggles, outside-click close, no console errors.
- **Task 57 (Online presence + chat)** — the OS is now online via the server-plugin: a tray **"N users online"** indicator (green dot + live count + unread badge on new messages) and the **Perch Chat** app (Start menu → Internet, or click the tray indicator). Server code is in index.html's `<script type="text/x-server-plugin">` block (public source, no secrets): a per-connection presence map broadcasting join/leave + count over a `presence` pub/sub topic, plus a bounded durable chat history (48-message ring in versioned `state`, evict-oldest, pure-JS utf8) relayed over a `chat` topic. Rate limits (per-conn 2 msgs/2s, per-`/24` 8 msgs/20s, tighter for proxies) with throttled "Slow down" notices; names change at most once/10s. `src/net.js` (`window.Net`) handles the socket: capped-exponential-backoff reconnect (no loop on the 4403 perchance.org-only closure), auto re-join + resync after server rebuilds, editable display name (defaults to the OS account's display name, saved in `webuntu.settings.chatName`), history fetch, own-messages-right alignment via `textContent`, and an offline/connecting status banner. **NOTE for future agents:** while the code is unsaved, `createServerSocket` runs the plugin's one-tab emulator (shows 1 user, no cross-tab multiplayer); real multiplayer needs the user to click save, then re-verify against the production server. Verified in the emulator: connect/join/history/echo, rename propagation, rate-limit notice, unread badge, close/reopen cleanup, phone layout, light theme.
- **Task 56 (Wallpaper pack + picker)** — the four original built-ins grew to **twelve**: eight new Rathji procedural wallpapers are all selectable from Settings → Appearance, the Welcome screen, and the desktop context-menu panel. **Aurora** (soft radial-gradient curtains + stars), **Planet** (ringed sphere, near-side ring in front, dashed orbits, moon), **Circuit** (PCB traces radiating from a central chip), **Stardust** (dense starfield + diagonal milky-way band), **Waveform** (layered sine surfaces + crest line), **Hex** (seamless honeycomb pattern tile), **Dusk** (CSS-token sunset, theme-aware), **Peaks** (moonlit ridges with violet rim-light). Generated art lives in `src/desktop.js` as deterministic seeded-PRNG SVG data-URIs re-derived from the active accent/theme (`svgDataUri`/`pal`/`mulberry32`; no SVG blur filters — rasterizes cheaply). The picker gained **Shuffle** (context panel) and **Random** (Settings) buttons. Verified: all 12 render cleanly in dark AND light themes, selection persists (`webuntu.settings.wallpaper`), Settings/Welcome pickers show all 12, shuffle/random apply + persist.
- **Task 55 (Minesweeper)** — the Task-48 "planned" stub is now a fully playable game. `src/minesweeper.js` registers the `minesweeper` `AppContent` builder (removed from the Software Center's planned list; `singleton`, so reopening focuses the one instance). Beginner 9x9/10 · Intermediate 16x16/40 · Expert 30x16/99; first-click-safe mine placement; left-click reveal with flood-fill, right-click flag→question→none (long-press flags on touch), chording, smiley reset with press-face, mines counter + timer, per-difficulty best times (`webuntu.ms.best`), and keyboard play. Win auto-flags mines and clears wrong flags; loss reveals all mines, red-marks the hit mine and wrong flags. Cells auto-size to the window via ResizeObserver; themed with CSS tokens. Verified: reveal/flag/chord logic, win/lose rendering, timer, best-time persistence.
- **Task 54 (First-boot Welcome screen)** — a one-time overlay appears ~0.7s after the first login: "Welcome to Webuntu 12 · Perch Mint" header, the live wallpaper picker (shared thumbnail row from the Control Center), four quick tips, a "Show on next login" checkbox, and a Get Started button (or Esc). `src/welcome.js` exposes `Welcome.check()`/`show()`/`dismiss()`; `session.js`'s `unlock()` calls `check()`. The `webuntu.welcomed` localStorage flag makes it one-shot per install — ticking the checkbox skips writing the flag so the next unlock shows it again. Fully theme-aware (CSS tokens) and responsive (card caps at 620px / 84vh, stacks on phones).
- **Post-52 polish batch (after the roadmap)** — **Task 53 (Trash bin)** — the virtual FS now has a root `/Trash` folder (new `FS.moveToTrash`/`restoreFromTrash`/`emptyTrash`/`isInTrash` API in `src/fs.js`): File Manager Delete moves items to Trash (confirm dialog wording changed accordingly), items keep their original path in `meta.trashOriginalPath`, and the Trash view offers per-item **Restore** (re-creates missing parent folders, de-duplicates names with a " (restored)" suffix) plus an **Empty Trash** button and a banner; deleting inside `/Trash` is permanent. Notes app deletes now go to Trash too (Terminal `rm` stays permanent). The desktop Trash shortcut now routes to `/Trash` via `kind=folder` (was an empty generic window), and `ensureSeeds` gained a `syncDesktopShortcuts` step so config changes land on already-stored trees. FS_VERSION bumped to 5. **Tasks 46–50 (Rathji shortcuts · Community shortcuts · Planned stubs · Responsive layout · Robustness)** — **Task 46 (Rathji tool shortcuts)** — all 15 Rathji picks from Appendix C (query-archive, vs-code-web, easy-uploads, file-format-converter, genesis-app, flux-reification-engine, voxelcraft, image-matrix, bgn-dnd-character-manager, bgn-dnd-source-reference, bgn-battle-map-forge, bgn-gm-battle-manager, experience-synthesia, ai-voice-assistant, atomic-roadmap) were already in the Task-45 `swcApps` catalog; **every one of the 46 existing slugs was verified live** against the perchance `getGeneratorsAndDependencies` API in one sweep (0 dead). **Task 47 (Community shortcuts)** — the official plugin pages (ai-text-plugin, text-to-image-plugin, plugins) were already present; added six verified-live community utilities from Appendix D slots: Dice Roller (dice-roller), Password Generator (password-generator), Markdown Editor (markdown-editor), Random Name Generator (random-name-generator), Color Tools (color-picker), Emoji Picker (emoji-picker). No live QR generator exists (`qr-code` is an empty shell), so QR Code Generator ships as the first **coming-soon** catalog entry: new `comingSoon`/`planned` fields on `swcApps` + Software Center states — a coming-soon card shows a dimmed tile and a disabled "Soon" Visit button; a planned card (dashed border) shows an "In development" badge with no action buttons. **Task 48 (Future-planned stubs)** — Weather, Mail, Minesweeper and PDF Viewer appear in the Software Center under a new **In Development** category (`swcCategories.planned`) as browsable-but-not-launchable cards, AND as `stub:true` entries in `appCatalog` so they show a "Soon" badge in the Start menu and open the themed "ships in a later build phase" window. **Task 49 (Responsive layout)** — verified at 390×844, 768×1024 and 1920×1080. Taskbar stays bottom-anchored and usable; windows clamp to the viewport; desktop icons auto-arrange; Start menu renders 2 app columns on phones. Two fixes: (a) the Software Center is now container-query responsive (`container-type:inline-size` + `@container` rules) — below 720px the sidebar collapses to a horizontal scrollable category strip and the grid shrinks; below 520px cards go single-column with readable titles; (b) a real WM bug: `createWindow` clamped spawn `x` against `vw() − MIN_W` instead of `vw() − w`, so a window wider than the viewport (e.g. a 920px Software Center on a 768px screen) opened with its right edge off-screen — now clamped to `vw() − w`. **Task 50 (Robustness)** — stress-tested: 18 simultaneous windows across 10 app types (taskbar shows all 18), 15× rapid open/close of Calculator, 6× rapid open/close of the Browser with an embed navigate — no freeze; dead shortcut targets never crash (unknown appId → generic singleton window, dead link → new tab, folder shortcut to a missing path → themed "Folder not found" window, coming-soon handled by Task 40); `localStorage` disabled via preamble → OS boots on an in-memory FS with the designed "running in memory only" note. All runs console/perchance/syntax error-free. **Tasks 41–45 (Video Games folder · VGN game launch · VGN hub · Developer templates · Software Center)** — **Task 41 (Video Games folder)** — `src/fs.js` `seedVideoGames()` (idempotent, wired into `ensureSeeds`/`buildDefaults`, FS_VERSION→4) fills `/home/user/Video Games` with 4 era subfolders (Network, Arcade, 8-Bit, Early 3D) and 25 curated game shortcuts taken from the *real* Video Game Network catalog (curation lives in `main.pjs` as `vgnEras` + `vgnGames`; each game: slug/title/icon/color/era/genre/year/players/comingSoon). Game shortcuts carry `meta.era`/`eraName`/`year`/`genre`; `src/filemanager.js` gained `arcadeTile(tile,item)` (adds `.tile-vgn` — marquee gradient + CSS scanline overlay — when `item.meta.era`) and `gameChip(item)` (VGN: "Era · Year", year hidden when "TBD"; BGN: players), used by both grid and list views; index.html gained the `.tile-vgn`/scanline CSS. The default "Video Game Network" shortcut is `kind:"hub"` (target `vgn-video-game-network`) and the Starfall Odyssey stub carries `hubName`/`hubSlug`. **Task 42 (VGN game launch)** — `src/gamelauncher.js` `openEmbed` gained an arcade mode: with `opts.arcade` (true when `m.era`) the window gets class `gl-arcade` + a per-game `--gl-tint` neon color driving a gradient title bar/marquee glow (index.html `.gl-arcade` CSS); an era chip (hidden unless set) joins the toolbar chips; `comingSoon()` now uses `m.hubName` ("Video Game Network") + `m.hubSlug` and gives the dialog tile the `.tile-vgn` treatment. **Task 43 (VGN hub shortcut)** — the pinned "The Video Game Network" hub shortcut routes through `Browser.navigate(vgn-video-game-network)` (singleton browser embed) with the always-available ↗ new-tab fallback, same as the BGN hub. **Task 44 (Developer templates)** — `seedDeveloper()` fills `/home/user/Software/Developer` with 19 template link-shortcuts (9 VGN + 10 BGN, from `main.pjs` `vgnTemplates`/`bgnTemplates`; fields slug/title/icon/color/blurb) plus a README file explaining the templates; the `dev-template-studio` catalog entry is now `type:"folder"` target `/home/user/Software/Developer` so the Start menu opens the File Manager there; double-clicking a template link opens the top-level page in a new tab. **Task 45 (Software Center)** — new `src/softwarecenter.js` (`window.SoftwareCenter`, registered as `window.AppContent["software-center"]`, 920×640, min 620×440). Windowed catalog with **Installed**/**Available** tabs, a live search field, a category sidebar (All + the 6 `swcCategories`), and cards with icon tile / title / source tag / blurb / action. **Installed** groups the 14 real `appCatalog` apps by their category (Open action → `Apps.launch`); **Available** lists 46 external generators (`main.pjs` `swcApps`: slug/title/icon/color/category/source(Rathji|BGN|VGN|Community)/blurb) across Tools/AI & Images/Games & RPG/Audio & Music/Developer/Community Hubs, each with **Visit** (open inside the Browser via `Browser.navigate`) and **↗** (top-level new tab). Fix during this pass: the Available grid was never populated on first paint (render only ran on category/search input) — it now renders on load. **Tasks 36–40 (System Monitor · Board Games folder · Game launcher · BGN hub · Coming-soon handling)** — **Task 36 (System Monitor)** — new `src/systemmonitor.js`, the windowed `system-monitor` app (singleton, 580×640). Fake-but-live readings: CPU as 4 cores with a seeded random walk (per-core % + a 5-min activity graph), RAM (base 2.1 GB + 0.22 GB per open window, walking toward its target), network up/down KB/s with a live canvas sparkline (down=cyan, up=purple), uptime since boot, open-window count, and a process list — one entry per `WM.windows` window with stable per-window fake CPU/RAM (a `procState` map). Timer is `setInterval` (200 ms) rather than rAF because the preview iframe is hidden; `onCloseRequest` clears the interval. **Task 37 (Board Games folder)** — `src/fs.js` `seedBoardGames()` (idempotent, wired into `ensureSeeds`/`buildDefaults`, FS_VERSION→3) fills `/home/user/Board Games` with 5 genre subfolders (Strategy, Card, Word&Pen&Paper, RPG, Reference & Tools) and 32 curated game shortcuts curated from the *real* Boardgame Network catalog (curation lives in `main.pjs` as `bgnGenres` + `bgnGames`; each game: slug/title/icon/color/players/genre, 4 marked `comingSoon` — all-aboard, outbreak-protocol, charterstone, alpha-the-gathering). Game shortcuts carry `meta.players` shown as a `.fm-chip` pill in the File Manager's grid and list views. **Task 38 (Game launcher)** — new `src/gamelauncher.js` (`window.GameLauncher`): `open(node)` dispatches coming-soon games to the dialog, everything else to `openEmbed(slug,opts)` — a windowed game embed (appId `"game:"+slug`, 900×620) with a toolbar (Reload ⟳ / Open in new tab ↗ / Close ✕), a players chip, and an iframe to `https://null.perchance.org/<slug>`, plus a `.gl-loading` overlay that falls back after 12 s to a "slow load" pane (Open in new tab / Back to games); closing the window resets the iframe src so embedded audio stops. **Task 39 (BGN hub shortcut)** — the default "Boardgame Network" shortcut is now `kind:"hub"` (target `bgn-boardgame-network`); `Launcher.openHub`/Start-menu switch case route it through `window.Browser.navigate(slug)` (singleton browser reused, `src/browser.js` exposes `Browser.navigate/getWindow` and `b.go`/`b.reload`/`b.goHome` on the browser object). **Task 40 (Coming-soon handling)** — coming-soon games open a themed `.gl-dialog-wrap` overlay (tile, "Soon" badge, blurb, players chip, **Back to games** dismiss / **Visit the hub instead** → `Browser.navigate`); coming-soon desktop icons show a `.desk-soon` badge + "name — Coming soon" tooltip; File Manager shows a "SOON" chip on their tiles. `src/launcher.js` now checks `comingSoon` before launching. **Tasks 31–35 (Terminal · Image Viewer · Music Player · Browser · Notes)** — five new windowed apps, each registered via `window.AppContent` in `main.pjs`'s catalog (all their `stub:true` flags dropped; Terminal/Music Player/Browser/Notes `singleton:true`, Image Viewer `singleton:false` so several images can be open at once). **Task 31 (Terminal)** — `src/terminal.js` (catalog `terminal`, 🧮-free ⌨️, singleton): the "Perch shell", a windowed terminal operating on the *real* virtual FS via `window.FSPath`. Commands: `ls` (dirs with `/`, shortcuts with `*`, `-l` long listing), `cd`, `pwd`, `cat`, `mkdir`, `rm`, `touch`, `echo` (with `>`/`>>` redirect into files), `clear`/`cls`, `help`, `history`, `about-webuntu` (neofetch-style ASCII block + OS/Desktop/Kernel/Shell lines from `root.distro`) and `neofetch` alias, `open <name>` (launches apps by catalog id, or shortcuts/folders by name from the cwd or home), plus `whoami`/`hostname`/`date`/`uname`/`version`/`exit`. History via ↑/↓, Tab autocompletes commands and cwd entries (single match fills, multiple match prints candidates), Ctrl+L clears, Ctrl+C cancels the input line. Dark terminal styling (near-black bg, cyan prompt showing `user@webuntu:~$`, monospace). **Task 32 (Image Viewer)** — `src/imageviewer.js` (`window.ImageViewer`): a windowed viewer with fit / 1:1 / zoom +/− / wheel-zoom / drag-pan / double-click-to-fit, an address bar that accepts any image URL or `wallpaper:<name>` (renders the built-in wallpapers as inline SVGs painted from the *current* theme tokens), prev/next navigation, a graceful error state for unloadable images, and keyboard shortcuts (+/− zoom, 0 fit, ←/→ prev/next) routed only while the viewer window is focused. `ImageViewer.openPath(path)` opens an FS image file (a file whose `meta.content` is an image URL) and collects its sibling image files for prev/next; `src/filemanager.js`'s `activate` now routes image files (URL content, non-.txt) to it. The Start-menu launch opens the Radial Glow wallpaper as a demo. **Task 33 (Music Player)** — `src/musicplayer.js`: a windowed player whose four built-in tracks ("Perch Fields", "Retro Orbit", "Night Currents", "Pixel Rain") are **synthesized live with WebAudio** — no audio assets. A lookahead scheduler (setInterval) plays chord-derived note patterns (bass/pad/arp/lead voices per track, built from progressions like Am–F–C–G) through oscillators with per-note envelopes. Controls: prev/play-pause/next, a live progress bar (pattern position; custom URL tracks use `<audio>` timeupdate), a per-track volume slider, and a playlist UI (track name + built-in/URL tag + duration, click to select, active highlight). "Add URL" appends a custom audio track (an `<audio>` element, looped). Output level follows the **global volume setting** (`webuntu.settings.volume`), i.e. it respects the OS sound level — muting the tray volume silences the player; closing the window stops playback. **Task 34 (Browser)** — `src/browser.js`: an address-bar browser that embeds any Perchance generator via the null-perchance URL (`https://null.perchance.org/<slug>` — platform auto-redirects to the real subdomain). Back/Forward/Reload/Home, an always-available "Open in new tab" (↗) fallback to the top-level page, and a local start page (`browser://home`) with quick-link cards (BGN hub, VGN hub, Plugins, ai-text-plugin, text-to-image-plugin, query-archive, vs-code-web, easy-uploads, file-format-converter, atomic-roadmap). The address bar accepts full URLs, `perchance.org/<slug>` or a bare slug. **Task 35 (Notes)** — `src/notes.js`: a two-pane notes app storing plain `.txt` notes in the virtual FS at `/home/user/Documents/Notes` (so they appear in the File Manager and open in the Text Editor too; the folder is created on first launch if missing). Left: note list (title + last-modified) + "＋ New Note". Right: title field, body editor, Save/Delete. Notes auto-save ~800ms after you stop typing (title renames the file via `FS.rename`), Ctrl+S saves immediately, Delete asks for confirmation in a themed dialog. **Task 30 (Calculator)** — new `src/calculator.js` (registered as the content builder for the `calculator` catalog entry; its `stub:true` flag dropped, `singleton:true` added so re-launching just focuses the window). A windowed scientific calculator with a live display: a large right-aligned value line plus an expression line (`12 + 7 =`), and a small cyan "M" indicator when memory holds a value. Layout: memory row (MC/MR/M+/M−), function keys (AC, √, ±, %, ⌫) and a 5-column keypad with a tall gradient "=" key and a wide "0" — all via CSS grid areas, themed like the rest of the OS (operators cyan, functions violet, AC danger-red, "=" the Rathji gradient). Engine: classic chaining state machine (entry/acc/pending-op/waiting flags, 15-digit cap, float noise trimmed to 12 significant digits with exponential fallback for huge/tiny magnitudes). Behavior: `%` is context-aware (`200 + 10%` → `220`; `250 × 10%` → `25`), `√` of a negative shows Error, `±` toggles sign (and starts a negative entry after an operator), AC resets, `⌫` deletes the last digit, and **divide-by-zero shows "Error"** (never Infinity/NaN) — recovery via any digit or AC. Memory: M+ adds the displayed value, M− subtracts, MR recalls, MC clears. Keyboard input works whenever the calculator window is focused (digits, `.`, `+ − * /`, Enter/=, Esc=clear, Backspace, `%`, `S`=√) via a single shared document-level handler that routes only to the focused calculator window. **Task 29 (Text Editor)** — new `src/texteditor.js` (`window.TextEditor`), the windowed plain-text editor over the virtual FS, registered as the content builder for the `text-editor` catalog entry (its `stub:true` flag dropped, `singleton:false` so each Start-menu launch opens a fresh untitled document; real files get their own window via `appId "text-editor:<path>"`, so several files can be open at once and re-opening one focuses its window). Toolbar: New (Ctrl+N — clears the current buffer in place after an unsaved-changes guard), Open… (Ctrl+O), Save (Ctrl+S), Save As… (Ctrl+Shift+S) and Find (Ctrl+F). Open/Save use a themed folder-navigator dialog (breadcrumb path, ↑ parent, folders-first list, filename field, overwrite confirmation) rooted at `/home/user/Documents`. Features: multiline editing, snapshot-based undo/redo (Ctrl+Z / Ctrl+Y, coalesced so continuous typing is one undo step, 100 steps), live status bar (Ln/Col from cursor, word/char/line counts, file size, current path), an in-file search bar (live match count, ▲/▼ next/prev, Enter/Shift+Enter, Esc — scrolls the match into view), and an unsaved-changes guard: closing the window (`WM.close` now awaits a new `onCloseRequest` hook) or starting New/Open while dirty prompts Save / Don't Save / Cancel in a themed dialog. Files saved to the FS update `meta.content`/`size`/`modified` and fire `onChange` (persistence). `src/filemanager.js`'s `activate` now routes plain-text files (string `meta.content`) to `TextEditor.openPath` instead of the read-only preview — the preview remains for non-text files. **Task 28 (File Manager)** — new `src/filemanager.js` (`window.FileManager`), the windowed app over the virtual FS, registered as the content builder for the `file-manager` catalog entry (its `stub:true` flag dropped, `singleton:true` added). Toolbar: back/forward/up navigation with a history stack, clickable breadcrumbs, a "New" menu (folder / text file) and a grid/list view toggle (view mode persists in localStorage). Item area: single + multi selection (click / ctrl-click / shift-range / arrow keys / Ctrl+A), double-click open (folders navigate, shortcuts launch via `Launcher`, files open a read-only preview window with a "Text Editor comes later" note), F2 rename, Delete via a themed in-window confirm dialog (no native confirm/alert), and right-click menus (Open / Rename / Delete on items; New folder / New text file on the empty background). Empty-state ("This folder is empty" + create buttons) and folder-not-found error state (with "Go to Home"). Folders sort folders-first then files/shortcuts, alphabetically; tile colors come from a per-icon map for the well-known home folders. `src/fs.js` gained `rename()` + `sanitizeName()` (shared validation, fires `onChange` so renames persist) and `src/launcher.js`'s `openFolder` now delegates to `FileManager.openPath` — a single file-manager window is reused and navigated to the target path (desktop folders and folder-kind shortcuts both benefit). Window title tracks the current folder via `WM.setTitle`. **Task 27 (UI sounds)** — new `src/sounds.js` (`window.Sounds`): a tiny WebAudio synth engine (no audio assets) producing subtle UI feedback — `open` (soft rising chirp on window/app open and window restore), `close` (soft falling chirp on window close), `error` (two low detuned buzzes on login/create failures), `startup` (rising C5·E5·G5·C6 chime queued at boot), plus a `notify` blip for the Settings toggle preview. All sounds are **off by default** (`webuntu.settings.uiSounds`) and gated behind the autoplay policy: the `AudioContext` is only created/resumed on the first `pointerdown`/`keydown`, a sound requested before any gesture (the boot chime) is queued and released on that first interaction, and every sound scales with the OS volume (`webuntu.settings.volume`, default 70 — 0 = silent). Wiring: `src/wm.js` plays open/close in `spawn`/`close`/`restore` (covers every app/stub/folder window since they all route through `WM.open`); `src/session.js` plays error on sign-in/account-creation failures; `src/settings.js`'s Sound toggle preview routes through `Sounds.play("notify")`; `src/systembar.js`'s volume-slider blip now goes through `Sounds.blip()` — deliberately *not* gated by the uiSounds toggle (it is the volume indicator itself), but still volume-scaled and interaction-gated. Loaded right after theme.js in index.html; boot chime queued in both boot paths (normal finish + skipBoot). **Task 26 (Accessibility)** — completed the remaining chrome text-size coverage (every readable chrome font converted from px to `rem` so the S/M/L setting scales it — start button, tray popup labels/vol %, tray power buttons, login screen, boot screen, power/suspend screens, help dialog, coming-soon/folder windows; only decorative glyph/logo sizes stay px); a global focus safety net in `index.html` — a `:focus-visible { outline:2px solid var(--accent2) }` rule catches any control without its own focus style (menu/dialog containers suppress it since their active item shows focus), plus a `@media (hover: none)` plain-`:focus` fallback because touch-first devices have unreliable `:focus-visible` heuristics; keyboard-operability polish — Start menu "All apps" grid gained arrow-key navigation (Left/Right one tile, Up/Down a row, wrapping), tray volume popup now focuses the slider on open, tray power menu focuses its first button, and everything else (context menus, Start menu, wallpaper picker, tray popups, help dialog) was already Tab/Enter/Esc-operable. Reduce-motion (`.reduce-motion, .reduce-motion * { transition:none!important; animation:none!important }`) and S/M/L text scaling were already in from Task 23 and re-verified. **Task 25 (Wallpaper picker)** — four built-in Rathji wallpapers, all accent/theme-driven so they re-tint live with the selected accent and work in both dark and light: **Radial Glow** (the original token wallpaper, now `--wp-radial`), **Gradient** (`--wp-gradient`, a violet→cyan→bg diagonal), **Grid Pattern** (`--wp-grid`, an SVG grid-line tile over two accent glows), and **Network Hub** (`--wp-network` is generated in `src/desktop.js` as a deterministic data-URI SVG — hub + 12 nodes + web ring, painted from the *current* accent/accent2/theme colors at apply time). `index.html` `:root` now declares these as tokens with `--wallpaper:var(--wp-radial)` as default in both themes. `Desktop.applyWallpaper()` accepts a builtin name, a URL (wrapped `url()…center/cover`) or a raw CSS value, and still persists under `webuntu.settings.wallpaper`; the `#desktop` and `#login` screens share the token so they re-paint together. New `src/wallpapers.js` (`window.Wallpapers`) owns the picker UI: a floating panel (`show(x,y)`, Esc/outside-click dismissal, clamped to viewport) wired to the desktop context-menu "Change Wallpaper…" item, plus a reusable live thumbnail row (`thumbnailsRow`) that the Control Center's Appearance→Wallpaper card embeds — each builtin gets a preview tile (`.set-wp-thumb`, generated background via `Desktop.wallpaperThumbBg`), the current choice is highlighted, a custom-image URL input + Apply accepts `http(s):/data:/blob:` links, and "Reset to default" restores the radial glow. **Task 24 (Theme engine)** — full-surface accent support on top of Task 23's swatch UI. Every accent-colored surface in `index.html` now reads `rgba(var(--accent-rgb), …)` instead of hardcoded violet, so a selected accent re-tints windows (focused border/glow, title-bar tint), the Start button/menu ring, context menus, the Alt-Tab strip, the wallpaper's primary radial, the power-screen glow and the token-derived glows (`--shadow-glow`, `--btn-shadow`) in both themes; the wallpaper's secondary radial reads `rgba(var(--accent2-rgb), …)` (light theme gained its own `--accent2-rgb`). `src/theme.js` `setAccent()` also flips the secondary color back to violet when the chosen accent is cyan/blue-ish (so the Rathji gradient stays a distinct two-tone), and clears those overrides when a default accent is restored. Accent persists via `webuntu.settings.accent`, applies at boot, and `?theme=` still works. **Task 23 (Control Center)** — new `src/settings.js` (`window.Settings`) is the windowed Settings app ("Control Center"), registered as the content builder for the `settings` catalog entry via `window.AppContent` (new hook in `src/apps.js`: real apps can register a builder that returns a DOM node or `{content, w, h, minW, minH}`; `settings` is no longer `stub:true` in `main.pjs`). Sidebar sections (Appearance / Personalization / Sound / System), every control applies instantly + persists in `webuntu.settings`: **theme** (dark/light via `Theme.apply`), **accent** (5 Rathji swatches + custom hex → new `Theme.setAccent()` in `src/theme.js` overrides the whole accent token family — `--accent`, `--accent-rgb`, `--ring`, `--selection-bg/--selection-border`, `--grad` — as inline props on `:root`, applied at boot; full swatch polish is Task 24), **text size** S/M/L (`--text-scale` token on `:root` + `html[data-text-size=s|l]`; the key chrome surfaces — window titles, taskbar entries, desktop labels, start-menu items, context menus, `.wm-body`, tray clock — were converted from px to em so they actually scale; Task 26 deepens), **reduce motion** (`.reduce-motion` class kills every transition/animation), **wallpaper** (status row; "Reset to default"; full picker deferred to Task 25), **desktop icons** toggle (`Desktop.setIconsVisible()` in `src/desktop.js`, `html.icons-off` CSS rule hides the grid since display:grid would beat the `hidden` attribute), **taskbar position** bottom/top (`html[data-taskbar="top"]` flips the taskbar + Start menu + tray popups + Alt-Tab strip; `src/wm.js` gained a `workTop()` helper so maximize-fill, spawn, drag/resize/clamp all offset below a top taskbar, plus a public `WM.layout()` to re-geometry open windows on switch), **UI sounds** toggle (persists `webuntu.settings.uiSounds`; small WebAudio preview blip on enable; real sound system is Task 27), **About** (`root.distro` card), **storage note** (`FSPersist.note()`), **Reset desktop…** (confirm → `FSPersist.resetDesktop()` which now also clears the accent override and re-applies default settings). `Settings.applyAll()` re-applies every stored setting at load and after a reset. Desktop context-menu "Change Wallpaper…"/"Display Settings" now open the real app. **Task 22 (Shortcut nodes)** — `src/fs.js` gained a `shortcut()` node factory (`{name, type:"shortcut", icon, color, meta:{kind: app|game|link|stub|folder, target, appId, singleton, comingSoon}}`; "folder" is a local extension of the spec's type list used by the desktop's folder launchers). New `src/launcher.js` (`window.Launcher`) is the single double-click dispatch: app → `Apps.launch(appId)` (unknown ids like Trash fall back to a generic singleton window), link/game → `https://perchance.org/<target>` new tab (Game Window embed is Task 38), stub → themed Coming-soon window (Task 40 refines), folder → themed window listing children (real File Manager is Task 28). The desktop is now **FS-backed**: `src/desktop.js` renders the shortcut/folder nodes of `/home/user/Desktop` (seeded from `main.pjs` `desktopDefaults`, which gained explicit `kind`/`target` fields), and `fspersist.js` seeds a Desktop folder into trees stored before Task 22 (migration persists once). Default tree also seeds hub shortcuts — Board Games: Boardgame Network → bgn-boardgame-network; Video Games: Video Game Network → vgn-video-game-network + Starfall Odyssey (stub); Software: Terminal/Settings/Notes/Software Center app shortcuts. **Task 21 (Persistence)** — `src/fspersist.js` (`window.FSPersist`): FS trees persist per user under `webuntu.fs.<username>` (auto-save hooked as `window.FS.onChange`, fired by fs.js after create/remove/reset); the stored tree is loaded at boot and reloaded on unlock, so signing in as another user swaps to their tree. `resetDesktop()` wipes the user's FS + `webuntu.settings` and restores defaults (also reachable from the desktop context menu "Reset Desktop…"). Falls back to in-memory with a `note()` if localStorage is unavailable. Wallpaper choice now persists too — `src/desktop.js` gained `Desktop.setWallpaper/getWallpaper/clearWallpaper` (image URLs become covering backgrounds; stored in `webuntu.settings.wallpaper`, applied at boot; picker UI is Task 25). **Task 20 (Path navigation)** — `src/fspath.js` (`window.FSPath`), a shared navigation layer for File Manager / Terminal / Save dialogs. Pure path-string math: `normalize`, `isAbsolute`, `join`, `basename`, `dirname`, `cd` (POSIX-ish: no arg or "~" → home, absolute → itself, else relative to cwd), `parse`, `parentPath`, `childPath` — collapses "//", resolves "."/"..", clamps ".." at root for absolute paths. FS-aware wrappers over `window.FS`: `lookup(path,{cwd})` → `{ok,node,path}` or `{ok:false,error}` (ENOENT/ENOTDIR), `cdNode`, `parent`/`child` node lookup, `homePath` (relative paths default to `/home/user`). **Task 19 (Filesystem model)** — `src/fs.js` (`window.FS`): an in-memory tree rooted at `/` with the per-user home `/home/user` holding the seven predefined folders (Board Games 🎲, Video Games 🕹️, Documents 📄 + Notes 📓 subfolder, Pictures 🖼️, Music 🎵, Downloads ⬇️, Software 💿), seeded with a few sample files. Every node is `{name, type: folder|file|shortcut, icon, meta}` (folders carry `children`; file content/size in meta), kept acyclic + JSON-serializable via a WeakMap parent table. API: `resolve`, `getPath`, `getParent`, `isFolder/isFile/isShortcut`, `exists`, `list`, `home`, `create`, `remove`, `reset`. Path parse/cd semantics are formalized next (Task 20), shortcut nodes in Task 22, persistence in Task 21. **Task 18 (Keyboard shortcuts)** — `src/shortcuts.js` (`window.Shortcuts`), the central shortcut suite: Super toggles the Start menu (moved here from startmenu.js), Alt+Tab / Shift+Alt+Tab cycle visible windows through a small visual switcher strip (z-order–based so it lands on the next-most-recent window; ends on Alt release), Ctrl+W closes the focused window (intercepted globally so the browser tab never closes), Esc closes menus/dialogs, and F1 / Ctrl+/ / Super+/ opens a themed help dialog documenting all shortcuts; all shortcuts are inert while locked. **Task 17 (Context menus)** — `src/contextmenu.js` (`window.ContextMenu`), a reusable themed right-click menu (`show(x,y,items)`) that clamps to the viewport and dismisses on outside click, Esc, scroll or resize, with arrow-key/Enter keyboard navigation. A single document-level `contextmenu` dispatch replaces the browser menu in the three shell zones: desktop → Arrange Icons, Change Wallpaper… (→ Settings stub for now), Open Terminal, Display Settings; taskbar → Lock, Settings; window title bars → Minimize / Maximize-Restore / Close (danger-styled); app content areas keep the browser default for future apps to override. **Task 16 (Power menu)** — `src/powermenu.js` (`window.PowerMenu`), the single dispatch for every power action used by both the Start menu and the tray power menu: Lock (Task 4 login), Restart (animated "Restarting Webuntu…" screen then reload → boot replays), Shut Down (confirm dialog, then animated "Shutting down Webuntu…" screen + reload), and a Suspend easter egg (full-screen dim with moon icon, click anywhere or Esc to wake); respects prefers-reduced-motion. **Task 15 (System bar)** — `src/systembar.js` (`window.SystemBar`): pinned-app launcher in the taskbar (configurable via the `pinnedApps` id list in main.pjs; running = small dot, focused = gradient underline), a tray with a live clock (time + date), decorative network/battery icons, a volume popup with a working slider (short WebAudio blip, persisted to `webuntu.settings.volume`), and a tray power menu (minimal Lock/Restart/Shut Down — the animated Power menu is Task 16); popups dismiss on outside-click/Esc and close each other + the Start menu. **Task 14 (Start menu)** — `src/apps.js` (`window.Apps`) + `src/startmenu.js` (`window.StartMenu`) + `appCatalog` list in main.pjs (18 apps; fields id/name/icon/color/category/blurb + optional type=link&target / singleton / stub). Menu: two-pane panel (header w/ user + live search; sidebar of 7 categories; main area with recently-launched list + "All apps" grid; footer Power button); stub apps show a "Soon" badge and launch a themed Coming-soon window; links open the top-level perchance page; real apps reuse Task-13 singleton semantics via `WM.open`; recent list persisted in localStorage (`webuntu.recent`); dismiss on outside-click/Esc; Super key toggles (skipped on macOS); minimal power actions (Lock/Restart/Shut Down — the animated Power menu is Task 16). Task 13 (multi-window edge cases, in `src/wm.js`): diagonal cascade spawn with a monotonic counter + occupancy skip so windows never stack exactly on top (each title bar peeks out); windows never permanently off-screen — spawn/drag clamps plus `clampIntoView` on viewport resize & unmaximize keep every title bar reachable; per-app re-open semantics via `appId` + `singleton` on `WM.open()` — singleton apps (Software Center, Trash) focus their existing window on re-open, multi-instance apps (folders) spawn a second instance. Earlier tasks: distro identity; boot sequence (animated progress + cycling log, skip via `?skipBoot=1`/`webuntu.settings.skipBoot`); desktop root (`src/desktop.js`, `desktopDefaults` icons, external link shortcuts launch in a new tab); lock/login screen (`src/session.js`, `window.OS.lock()/unlock()`) — now a full account system: create account (username+password, salted SHA-256 hash stored in `localStorage["webuntu.accounts"]`), sign in, multiple accounts, last-user remembered, and a "Reset accounts" escape hatch so it never traps the user; design tokens (`index.html` `:root` = full Rathji palette, dark default; `:root[data-theme="light"]` flips every color with zero layout change; `src/theme.js` applies persisted choice + `?theme=dark|light`, exposes `window.Theme.apply()/toggle()`); window model + registry (`src/wm.js`, `window.WM`): `{id,title,icon,w,h,x,y,minimized,maximized,closed,zIndex,content}` as single source of truth, arbitrary open windows, open/close/focus/minimize/restore/maximize/toggleMaximize, cascade spawn, z-order; window chrome: title bar with icon+title+min/max/close SVG buttons (close hover = red), dbl-click title toggles maximize (max button flips to restore glyph), focused style (violet border + glow + tinted title bar + accent title), isolated scrollable content with themed scrollbars; title-bar dragging (`pointerdown`/capture, live model update, clamped so the title bar stays on-screen and ≥80px of window stays visible, never fires on chrome buttons, maximized windows don't drag, "dragging" lift effect); resizing (`src/wm.js` — 8 edge/corner handles `wm-resize`, pointer-capture live resize, min size enforced, window kept within the desktop, opposite edge fixed when dragging left/top, handles hidden when maximized, chrome buttons layered above corner handles, `.resizing` state); focus & z-order (`src/wm.js` — mousedown anywhere on a window raises it to front, focused chrome distinct via `.focused`, `focusTop()` skips minimized windows so focus never lands hidden, desktop click clears focus); taskbar (`src/wm.js` + `#taskbar`/`#taskbarEntries` in index.html — one entry per open window in stable spawn order, icon+title, `.active` highlight with gradient underline, minimized entries dimmed, click toggles focus/minimize/restore, hover "✕" or middle-click closes, `#desktop` given its own stacking context (`z-index:0`) so the taskbar (`z-index:1000`) always stays above any window); minimize & maximize (`src/wm.js` — minimized windows hide from the desktop but keep their taskbar entry, maximize now fills the work area ABOVE the taskbar via the shared `--taskbar-h` token (`calc(100% - var(--taskbar-h))`), restore/unmaximize returns the prior geometry, restoring a previously-maximized window stays maximized); non-link desktop icons double-click to open a generic window.
 **Tasks 51–52 (Meta · Final visual QA)** — **Task 51 ($meta)** — title/description/tags/minimal header already existed; added `$meta.image` = a real desktop screenshot (File Manager on Board Games + Software Center open) hosted at user.uploads.dev (`c447174e333be859e2ca11cab3bd77cc.png`) for the social card. **Task 52 (Final visual QA)** — a vision-model pass across every screen: boot splash (scrolling log + progress), both login states (create-account + sign-in), dark desktop + icons, Start menu, all 10 app windows (File Manager, Text Editor, Calculator, Terminal neofetch, Image Viewer, Music Player, Browser, Notes, System Monitor, Software Center), Settings/Control Center, both game folders (Board Games tiles, Video Games arcade tiles + era subfolders), the light theme, and the shutdown + suspend screens — all clean against the Rathji palette (dark navy + violet/cyan/purple; boot-log green/yellow status markers and per-folder icon colors are intentional). During this pass the preview hit the known platform “wedged renderer” state (rAF stops firing while evals still answer) — a browser-tab reload clears it.
- Full roadmap + canonical appendices (BGN/VGN game inventories, Rathji shortcut sources, design tokens) live in `src/ROADMAP.md`.

## Architecture

- `main.pjs` — checklist, `$meta`, distro identity, pjs data lists + functions (imported into the page as globals/`root.*`).
- `index.html` — the OS shell: HTML + CSS + app logic (window manager, filesystem, apps get built here as phases complete).
- `src/` — durable docs; future durable assets (sprites, data) go here or via `upload_file`.

## Workflow

Work strictly one task at a time, top to bottom. After implementing a task: validate via page_eval/vision, mark it `[x]` in the main.pjs checklist, then stop for review before the next task.
