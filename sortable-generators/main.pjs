$meta
  title = Recently Sortable Generators

// Self-contained "Recently Updated Generators" page with built-in search & sort.
// All logic lives in index.html — it fetches /api/getGeneratorList and renders the
// generator cards, replicating perchance.org/generators but adding a filter box and
// a sort dropdown (recency, name, views).
