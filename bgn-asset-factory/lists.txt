// ─────────────────────────────────────────────────────────────
//  BGN ASSET FACTORY — main.pjs
//  A workshop for the Boardgame Network: design game assets —
//  pieces, cards, dice, chips, tiles, money, spinners and
//  boards — on the factory floor, then cut them into printable
//  sheets (PNG) or share the whole project (JSON).
//  All the building happens in index.html; this file just sets
//  page identity and theme.
// ─────────────────────────────────────────────────────────────

$meta
  title = BGN Asset Factory
  description = A workshop for the Boardgame Network — design game pieces, cards, dice, chips, tiles, money, spinners and boards, then cut them into printable sheets. Export PNG cut sheets or share your project as JSON.
  tags = board game, assets, pieces, cards, dice, chips, tiles, money, spinner, factory, workshop, print and play, boardgame network
  image = https://user.uploads.dev/file/d06957f186bba0884437499f74fdb43d.jpg

hubUrl = https://perchance.org/bgn-boardgame-network  // ← the network hub
gameTitle = BGN Asset Factory
gameTagline = A workshop for the Boardgame Network. Forge pieces, cards, dice and more, then cut them into printable sheets ready for your table.
gameKicker = Blueprint · Forge · Cut & Play
gameGenre = Tool
gamePlayers = Solo
accentColor = #d4af37
heroImage = https://user.uploads.dev/file/d06957f186bba0884437499f74fdb43d.jpg
