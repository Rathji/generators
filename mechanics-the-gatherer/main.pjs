// Mechanics the Gatherer
// A browsable compendium of tabletop game systems.
// All content lives in the game data embedded in index.html
// (parsed from the src/*-features.md reference files at build time).
// To add a game: create src/<game>-features.md in the same format,
// re-parse it, and re-embed into index.html.
