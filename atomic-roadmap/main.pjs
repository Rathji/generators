// Atomic Roadmap — transforms high-level project concepts into atomic,
// agent-ready task backlogs, gated behind a strict planning-first hold.
// UI + prompt assembly live in index.html; the system instructions live in
// src/system-prompt.js (kept out of pjs so its markdown is preserved verbatim).

title
  Atomic Roadmap

$meta
  title = Atomic Roadmap
  description = Transforms high-level software, game, or tool concepts into granular, atomic task backlogs formatted specifically for AI coding agents (Claude Code, Cursor, Copilot, etc.) with a strict planning-first gate.
  tags = ai, planning, backlog, tasks, roadmap, prompt engineering
  header
    mode = minimal

generateText = {import:ai-text-plugin}
