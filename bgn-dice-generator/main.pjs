// ─────────────────────────────────────────────────────────────
//  DIE FACE PLATES — main.pjs
//  Site config for the BGN-themed page (nav / hero / footer).
//  The app itself lives in src/app.jsx (→ src/app.bundle.js).
//  Edit hubUrl / projectsUrl / authorInitial to point at your own.
// ─────────────────────────────────────────────────────────────

$meta
  title = Die face plates
  description = Orthographic face plates for d4 through d20 — one shared lattice, so a set cuts together cleanly. Build your dice in the browser, export as PNG or SVG.
  tags = dice, generator, tool, svg, tabletop, board game
  image = https://user.uploads.dev/file/251e4955b48d8110273a62de3afc596f.jpg

hubUrl = https://perchance.org/bgn-boardgame-network  // ← SET THIS to your network hub's URL
projectsUrl = https://perchance.org/rathjis-generators  // ← SET THIS to your Perchance projects page
authorInitial = R  // initial shown on the "Rathji's Projects" badge
gameTitle = Die face plates
gameTagline = Orthographic face plates for d4 through d20 — one shared lattice, so a set cuts together cleanly. Flat-on geometry, drawn not generated.
gameKicker = Build · Export · Cut
gameGenre = Generator
gamePlayers = d2 – d20
accentColor = #d4af37
heroImage = https://user.uploads.dev/file/251e4955b48d8110273a62de3afc596f.jpg
