// Data Downloader / Host
// Required for hosting files to user.uploads.dev and getting back links.
uploadPlugin = {import:upload-plugin}
// Required for the proxied host provider (tmpfiles.org).
superFetch = {import:super-fetch-plugin}
