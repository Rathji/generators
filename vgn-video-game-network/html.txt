<!-- ════════════════════════════════════════════════════════════
     THE VIDEO GAME NETWORK — ARCADE HUB
     • Retro CRT / neon-arcade aesthetic (pixel fonts, scanlines,
       twinkling starfield hero, synthwave grid floor).
     • Cabinet cards are generated from the `games` list in main.pjs.
     • Realtime lobby (chat + presence + game-night matchmaking)
       runs on the server-plugin block below.
     ════════════════════════════════════════════════════════════ -->

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap" rel="stylesheet">

<style>
  :root{
    --bg:#07090f;
    --bg2:#0b101a;
    --panel:#10141f;
    --panel2:#0d1119;
    --neon:#25d7fd;
    --pink:#ff2bd6;
    --gold:#ffd319;
    --green:#3ddc84;
    --red:#ff5a3c;
    --text:#dce6f0;
    --muted:#8a93a5;
    --line:rgba(120,140,180,.14);
    --pix:'Press Start 2P',monospace;
    --term:'VT323',monospace;
  }
  *{box-sizing:border-box}
  html{scroll-behavior:smooth}
  body{
    margin:0;background:var(--bg);color:var(--text);
    font-family:var(--term);font-size:1.2rem;line-height:1.5;
    text-align:left;overflow-x:hidden;
    background-image:
      radial-gradient(1000px 600px at 85% -5%, rgba(37,215,253,.07), transparent 60%),
      radial-gradient(900px 620px at -10% 20%, rgba(255,43,214,.06), transparent 60%),
      radial-gradient(700px 400px at 60% 110%, rgba(61,220,132,.05), transparent 60%);
  }
  h1,h2,h3,h4{font-family:var(--pix);margin:0;font-weight:400}
  a{color:inherit}
  img{max-width:100%}

  /* ── CRT overlay (whole page) ── */
  .crt{
    position:fixed;inset:0;z-index:9999;pointer-events:none;
    background:
      repeating-linear-gradient(0deg, rgba(0,0,0,.13) 0 1px, transparent 1px 3px);
    mix-blend-mode:multiply;
  }
  .crt::after{ /* vignette */
    content:"";position:absolute;inset:0;
    background:radial-gradient(120% 90% at 50% 40%, transparent 55%, rgba(0,0,0,.5) 100%);
  }
  .crt-bar{ /* slow travelling scan bar */
    content:"";position:absolute;left:0;right:0;height:90px;top:-20%;
    background:linear-gradient(180deg,transparent,rgba(37,215,253,.05) 50%,transparent);
    animation:scanmove 9s linear infinite;
  }
  @keyframes scanmove{from{top:-20%}to{top:120%}}

  .wrap{max-width:1180px;margin:0 auto;padding:0 22px}

  /* ── NAV ── */
  nav{
    position:fixed;top:0;left:0;right:0;z-index:100;
    backdrop-filter:blur(10px);-webkit-backdrop-filter:blur(10px);
    background:rgba(7,9,15,.62);border-bottom:1px solid var(--line);
  }
  nav .wrap{display:flex;align-items:center;justify-content:space-between;height:60px;gap:14px}
  .nav-brand{
    display:flex;align-items:center;gap:10px;text-decoration:none;
    font-family:var(--pix);font-size:.6rem;letter-spacing:.12em;color:var(--text);
  }
  .nav-brand b{color:var(--neon);text-shadow:0 0 10px rgba(37,215,253,.7);}
  .nav-links{display:flex;gap:24px;font-family:var(--pix);font-size:.56rem;letter-spacing:.1em}
  .nav-links a{color:var(--muted);text-decoration:none;transition:color .25s,text-shadow .25s}
  .nav-links a:hover{color:var(--gold);text-shadow:0 0 12px rgba(255,211,25,.6)}
  @media(max-width:640px){.nav-links{display:none}}

  /* ── HERO ── */
  .hero{
    position:relative;height:100vh;height:100svh;min-height:580px;overflow:hidden;
    display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;
    background:radial-gradient(90% 80% at 50% 20%, #0b101a 0%, #07090f 75%);
  }
  #starfield{position:absolute;inset:0;z-index:0;width:100%;height:100%}
  .hero-floor{
    position:absolute;left:-25%;right:-25%;bottom:-6%;height:50%;z-index:1;pointer-events:none;
    background:
      linear-gradient(180deg,transparent 0%, rgba(7,9,15,.30) 38%, rgba(7,9,15,.96) 100%),
      repeating-linear-gradient(90deg, rgba(37,215,253,.22) 0 1px, transparent 1px 70px),
      repeating-linear-gradient(0deg, rgba(37,215,253,.20) 0 1px, transparent 1px 46px);
    transform:perspective(430px) rotateX(62deg);transform-origin:top center;
    -webkit-mask-image:linear-gradient(180deg,transparent 0%,#000 45%);
            mask-image:linear-gradient(180deg,transparent 0%,#000 45%);
  }
  .h-glyph{position:absolute;z-index:2;color:rgba(150,200,255,.14);font-size:2.6rem;user-select:none;pointer-events:none;animation:hfloat 9s ease-in-out infinite;animation-delay:var(--gd,0s)}
  @keyframes hfloat{0%,100%{transform:translateY(0) rotate(-6deg)}50%{transform:translateY(-22px) rotate(6deg)}}
  .hero-content{position:relative;z-index:3;padding:30px 26px;max-width:880px}
  .wordmark{display:inline-flex;align-items:center;gap:12px;margin-bottom:26px;font-family:var(--pix);font-size:.62rem;letter-spacing:.3em;color:var(--text)}
  .wordmark .wm{color:var(--neon);text-shadow:0 0 10px rgba(37,215,253,.8)}
  .kicker{font-family:var(--pix);font-size:.58rem;letter-spacing:.34em;color:var(--gold);text-shadow:0 0 14px rgba(255,211,25,.5);margin-bottom:20px;animation:soonblink 2.2s steps(2) infinite}
  .hero-title{font-size:clamp(1.7rem,6.5vw,3.7rem);line-height:1.5;animation:hflicker 5s infinite}
  .t1{color:var(--neon);text-shadow:0 0 18px rgba(37,215,253,.75),0 0 70px rgba(37,215,253,.3)}
  .t2{color:var(--pink);text-shadow:0 0 18px rgba(255,43,214,.75),0 0 70px rgba(255,43,214,.3)}
  .t3{color:var(--gold);text-shadow:0 0 18px rgba(255,211,25,.75),0 0 70px rgba(255,211,25,.3)}
  @keyframes hflicker{0%,91%,94%,97%,100%{opacity:1}92%{opacity:.55}93%{opacity:.9}95%{opacity:.35}96%{opacity:1}}
  .hero-sub{margin:20px auto 0;color:var(--muted);font-size:clamp(1.15rem,2.4vw,1.45rem);letter-spacing:.02em}
  .hero-cta{display:flex;gap:16px;justify-content:center;margin-top:34px;flex-wrap:wrap}
  .btn{
    font-family:var(--pix);letter-spacing:.14em;font-size:.66rem;text-transform:uppercase;
    padding:16px 28px;cursor:pointer;text-decoration:none;border:1px solid transparent;
    transition:transform .25s,box-shadow .25s,color .25s,border-color .25s;
  }
  .btn-insert{
    color:#07090f;background:linear-gradient(180deg,#ffe25e,#ffd319 55%,#f0a800);
    box-shadow:0 8px 28px rgba(255,211,25,.35), inset 0 1px 0 rgba(255,255,255,.6);
    clip-path:polygon(10px 0,100% 0,100% calc(100% - 10px),calc(100% - 10px) 100%,0 100%,0 10px);
  }
  .btn-insert:hover{transform:translateY(-3px);box-shadow:0 14px 40px rgba(255,211,25,.55), inset 0 1px 0 rgba(255,255,255,.6)}
  .btn-ghost{color:var(--neon);border-color:rgba(37,215,253,.45);background:rgba(37,215,253,.06);clip-path:polygon(10px 0,100% 0,100% calc(100% - 10px),calc(100% - 10px) 100%,0 100%,0 10px)}
  .btn-ghost:hover{transform:translateY(-3px);border-color:var(--neon);box-shadow:0 0 24px rgba(37,215,253,.25)}
  .press-start{
    position:absolute;bottom:22px;left:0;right:0;z-index:3;text-align:center;
    font-family:var(--pix);font-size:.56rem;letter-spacing:.3em;color:#5a6476;animation:soonblink 1.4s steps(2) infinite;
  }
  @keyframes soonblink{50%{opacity:.35}}

  /* ── MARQUEE ── */
  .marquee{overflow:hidden;border-top:1px solid var(--line);border-bottom:1px solid var(--line);background:rgba(13,17,25,.7);padding:13px 0}
  .marquee-track{display:flex;gap:0;white-space:nowrap;width:max-content;animation:scrollx 46s linear infinite}
  .marquee-track span{font-family:var(--pix);font-size:.56rem;letter-spacing:.16em;color:rgba(150,200,255,.45)}
  .marquee-track .star{color:rgba(255,211,25,.55)}
  @keyframes scrollx{from{transform:translateX(0)}to{transform:translateX(-50%)}}

  /* ── SECTIONS ── */
  section{padding:84px 0}
  .sec-head{text-align:center;margin-bottom:44px}
  .sec-eyebrow{font-family:var(--pix);font-size:.56rem;letter-spacing:.34em;text-transform:uppercase;color:var(--green);text-shadow:0 0 12px rgba(61,220,132,.5)}
  .sec-title{font-size:clamp(1.2rem,3.4vw,1.9rem);margin:16px 0 12px;letter-spacing:.04em}
  .sec-title .n1{color:var(--neon);text-shadow:0 0 16px rgba(37,215,253,.6)}
  .sec-title .n2{color:var(--pink);text-shadow:0 0 16px rgba(255,43,214,.6)}
  .sec-sub{color:var(--muted);max-width:600px;margin:0 auto;font-size:1.2rem}

  /* stats */
  .stats{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;max-width:820px;margin:0 auto 8px}
  .stat{
    position:relative;background:linear-gradient(180deg,rgba(37,215,253,.05),rgba(37,215,253,.01));
    border:1px solid var(--line);padding:26px 14px 22px;text-align:center;overflow:hidden;
    clip-path:polygon(12px 0,100% 0,100% calc(100% - 12px),calc(100% - 12px) 100%,0 100%,0 12px);
  }
  .stat::before{content:"";position:absolute;top:0;left:18%;right:18%;height:1px;background:linear-gradient(90deg,transparent,var(--neon),transparent)}
  .stat b{display:block;font-family:var(--pix);font-size:clamp(1.2rem,3vw,1.8rem);color:var(--gold);text-shadow:0 0 18px rgba(255,211,25,.45);margin-bottom:10px}
  .stat small{color:var(--muted);font-family:var(--pix);letter-spacing:.14em;text-transform:uppercase;font-size:.5rem;line-height:1.8}
  @media(max-width:640px){.stats{grid-template-columns:1fr 1fr 1fr}.stat{padding:18px 6px 14px}.stat small{font-size:.4rem}}

  /* ── FILTER BAR ── */
  #arcade{scroll-margin-top:74px}
  .filter-bar{display:flex;flex-wrap:wrap;gap:14px;align-items:center;justify-content:space-between;margin-bottom:30px}
  .search{
    flex:1 1 220px;background:rgba(13,17,25,.8);border:1px solid var(--line);color:var(--text);
    padding:12px 16px;font-family:var(--term);font-size:1.15rem;outline:none;
    transition:border-color .25s,box-shadow .25s;
  }
  .search:focus{border-color:var(--neon);box-shadow:0 0 0 3px rgba(37,215,253,.15)}
  .chips{display:flex;flex-wrap:wrap;gap:8px}
  .chip{
    font-family:var(--pix);font-size:.52rem;letter-spacing:.08em;text-transform:uppercase;
    border:1px solid var(--line);background:rgba(13,17,25,.6);color:var(--muted);
    padding:9px 14px;cursor:pointer;transition:all .22s;clip-path:polygon(6px 0,100% 0,100% calc(100% - 6px),calc(100% - 6px) 100%,0 100%,0 6px);
  }
  .chip:hover{color:var(--neon);border-color:rgba(37,215,253,.5)}
  .chip.on{background:linear-gradient(180deg,#ffe25e,#ffd319);color:#07090f;border-color:transparent;text-shadow:none}

  /* ── CABINET GRID / CARDS ── */
  .grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(255px,1fr));gap:22px}
  .cab{
    position:relative;display:flex;flex-direction:column;overflow:hidden;
    background:linear-gradient(180deg,#131a29,#0d1220);border:1px solid var(--line);
    transition:transform .3s ease,box-shadow .3s ease,border-color .3s ease;text-decoration:none;
  }
  .cab:hover{transform:translateY(-6px);border-color:var(--accent);box-shadow:0 20px 46px rgba(0,0,0,.5),0 0 26px var(--accentglow)}
  .cab-art{position:relative;aspect-ratio:16/10;overflow:hidden;background:#0a0e14}
  .cab-art img{width:100%;height:100%;object-fit:cover;display:block;transition:transform .5s ease}
  .cab:hover .cab-art img{transform:scale(1.07)}
  .cab-art::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(5,8,14,.05) 0%,rgba(5,8,14,.2) 75%,rgba(5,8,14,.62) 100%)}
  .cab-art .crt-lines{position:absolute;inset:0;z-index:1;background:repeating-linear-gradient(0deg,rgba(0,0,0,.14) 0 2px,transparent 2px 4px);mix-blend-mode:multiply}
  .cab-tags{position:absolute;top:10px;left:10px;right:10px;z-index:2;display:flex;justify-content:space-between;gap:8px;pointer-events:none}
  .tag{font-family:var(--pix);font-size:.5rem;letter-spacing:.08em;padding:5px 8px;background:rgba(5,8,14,.72);border:1px solid rgba(255,255,255,.16);color:#dce6f0;white-space:nowrap}
  .tag-soon{color:var(--gold);border-color:rgba(255,211,25,.55);animation:soonblink 1.7s steps(2) infinite}
  .tag-live{color:#06130e;background:var(--green);border-color:transparent}
  .cab-body{padding:14px 15px 15px;display:flex;flex-direction:column;gap:9px;flex:1}
  .cab-title-row{display:flex;align-items:baseline;justify-content:space-between;gap:8px}
  .cab-title{font-family:var(--pix);font-size:.66rem;line-height:1.6;color:var(--text);text-shadow:0 0 8px var(--accenttitle)}
  .cab-year{font-family:var(--term);font-size:1.05rem;color:var(--accent);white-space:nowrap}
  .cab-blurb{font-family:var(--term);font-size:1.12rem;line-height:1.32;color:var(--muted);flex:1}
  .cab-foot{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-top:2px}
  .chip{font-family:var(--pix);font-size:.48rem;letter-spacing:.1em;color:var(--accent);border:1px solid var(--accentline);padding:4px 8px;background:rgba(0,0,0,.32)}
  .players{font-family:var(--term);font-size:.95rem;color:#7c8598;white-space:nowrap}

  /* ── HOW TO JOIN ── */
  .sec-sub code{color:var(--neon);background:rgba(37,215,253,.08);border:1px solid rgba(37,215,253,.25);padding:1px 5px;font-family:var(--term);font-size:1rem}
  .join-steps{display:grid;grid-template-columns:repeat(4,1fr);gap:16px;margin-top:10px}
  .jstep{
    position:relative;border:1px solid var(--line);padding:22px 18px 18px;
    background:linear-gradient(180deg,rgba(37,215,253,.04),rgba(37,215,253,.01));
    clip-path:polygon(10px 0,100% 0,100% calc(100% - 10px),calc(100% - 10px) 100%,0 100%,0 10px);
  }
  .jstep .jnum{
    position:absolute;top:-13px;left:14px;font-family:var(--pix);font-size:.82rem;color:#07090f;
    background:linear-gradient(180deg,#ffe25e,#ffd319);padding:5px 9px;
    box-shadow:0 4px 14px rgba(255,211,25,.35);
  }
  .jstep b{display:block;font-family:var(--pix);font-size:.58rem;letter-spacing:.05em;color:var(--text);margin:10px 0 8px;line-height:1.7}
  .jstep p{font-family:var(--term);font-size:1.08rem;color:var(--muted);margin:0;line-height:1.42}
  @media(max-width:840px){.join-steps{grid-template-columns:1fr 1fr}}
  @media(max-width:520px){.join-steps{grid-template-columns:1fr}}
  .more-note{text-align:center;color:var(--muted);font-family:var(--term);font-size:1.12rem;margin-top:36px}
  .more-note b{color:var(--gold)}
  .coinbar{
    margin-top:2px;display:flex;align-items:center;justify-content:center;gap:8px;
    padding:8px 0 2px;border-top:1px dashed rgba(120,140,180,.13);
    font-family:var(--pix);font-size:.5rem;letter-spacing:.2em;color:#5a6476;
  }
  .coinbar .coin{color:var(--gold);font-size:.62rem;animation:soonblink 1.7s steps(2) infinite}
  .coinbar .playnow{color:var(--green);text-shadow:0 0 10px rgba(61,220,132,.6)}
  .empty-state{grid-column:1/-1;text-align:center;color:var(--muted);padding:70px 0;font-family:var(--pix);font-size:.68rem;line-height:2.2}
  .empty-state .big{font-size:2rem;margin-bottom:12px;color:var(--gold)}
  .subhead{grid-column:1/-1;margin:36px 0 6px;display:flex;align-items:baseline;gap:14px;flex-wrap:wrap}
  .subhead h3{font-size:.78rem;letter-spacing:.06em;color:var(--text)}
  .subhead h3 span{color:var(--accent,var(--neon));text-shadow:0 0 12px currentColor}
  .subhead small{font-family:var(--term);font-size:1.05rem;color:var(--muted)}
  .subhead::after{content:"";flex:1;height:1px;background:linear-gradient(90deg,var(--line),transparent)}
  .gridfade{animation:gfade .4s ease}
  @keyframes gfade{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}

  /* ── FOOTER ── */
  footer{border-top:1px solid var(--line);padding:46px 0 52px;text-align:center;background:rgba(7,9,15,.6)}
  .ornament{display:flex;align-items:center;gap:14px;justify-content:center;color:var(--neon);opacity:.8;margin-bottom:22px}
  .ornament::before,.ornament::after{content:"";height:1px;width:80px;background:linear-gradient(90deg,transparent,var(--neon));}
  .ornament::after{background:linear-gradient(90deg,var(--neon),transparent)}
  footer p{margin:6px 0;color:var(--muted)}
  footer .brand{font-family:var(--pix);font-size:.6rem;color:var(--text)}
  footer .brand b{color:var(--neon)}
  footer a{color:var(--gold)}
  footer .footnote{font-size:1rem;color:#565f72}

  @media (prefers-reduced-motion: reduce){
    .crt-bar{animation:none}.hero-title{animation:none}.kicker{animation:none}
    .marquee-track{animation:none}.press-start{animation:none}.tag-soon{animation:none}
    .cab:hover{transform:none}.btn-insert:hover{transform:none}.btn-ghost:hover{transform:none}
    html{scroll-behavior:auto}
  }
</style>

<!-- ════════════════════════════════════════════════════════════
     VGN LOBBY SERVER — realtime chat + game-night matchmaking.
     This block is PUBLIC source (runs authoritatively on the
     server, but anyone can read it) — keep no secrets in here.
     Durable `state` layout:
       [0]     version (1)
       [1..4]  writeCount (uint32 LE) — messages ever appended
       [5..]   ring of MAX_REC fixed records, REC bytes each
               record: ts(4) nameLen(1) name(64) msgLen(1) msg(512)
     Matchmaking pairs players in groups of 2 for any cabinet.
     ════════════════════════════════════════════════════════════ -->
<script type="text/x-server-plugin">
"use strict";
var VERSION = 1;
var REC = 4 + 1 + 64 + 1 + 512;
var MAX_REC = Math.floor((state.length - 5) / REC);
if (MAX_REC > 1500) MAX_REC = 1500;
var TARGET = 2;

var BANNED = ["nigger","nigga","faggot","kys","kill yourself","retard","heil hitler","tranny"];

var conns = new Map();
var queues = {};
var rateConn = {}, rateNet = {};
var lastSweep = Date.now();

self.onopen = function({conn}){
  try{ conn.subscribe("lobby"); conn.subscribe("presence"); }catch(e){}
};

function writeU32(o, v){ state[o]=v&255; state[o+1]=(v>>>8)&255; state[o+2]=(v>>>16)&255; state[o+3]=(v>>>24)&255; }
function readU32(o){ return ((state[o]|state[o+1]<<8|state[o+2]<<16|state[o+3]<<24)>>>0); }

if (state[0] !== VERSION){ state[0] = VERSION; writeU32(1, 0); }

function u8(s){
  var out=[], i, c; s=String(s);
  for(i=0;i<s.length;i++){
    c=s.charCodeAt(i);
    if(c<128){ out.push(c); }
    else if(c<2048){ out.push(192|c>>6, 128|c&63); }
    else if(c<55296||c>57343){ out.push(224|c>>12, 128|c>>6&63, 128|c&63); }
    else{ i++;
      var h=(c&1023)<<10 | s.charCodeAt(i)&1023;
      out.push(240|h>>18, 128|h>>12&63, 128|h>>6&63, 128|h&63);
    }
  }
  return out;
}
function d8(a, o, l){
  var s="", i=o, e=o+l, c;
  while(i<e){
    c=a[i++];
    if(c<128){ s+=String.fromCharCode(c); }
    else if(c>191&&c<224){ s+=String.fromCharCode((c&31)<<6|a[i++]&63); }
    else if(c>239){ var u=(c&7)<<18|(a[i++]&63)<<12|(a[i++]&63)<<6|a[i++]&63; s+=String.fromCharCode(55296+(u>>10), 56320+(u&1023)); }
    else{ s+=String.fromCharCode((c&15)<<12|(a[i++]&63)<<6|a[i++]&63); }
  }
  return s;
}

function clean(s, max){ return String(s||"").trim().replace(/[\u0000-\u001f\u007f]/g, "").replace(/\s+/g, " ").slice(0, max); }
function isBanned(t){ var s=t.toLowerCase(); for(var i=0;i<BANNED.length;i++){ if(s.indexOf(BANNED[i])!==-1) return true; } return false; }
function nowSec(){ return Math.floor(Date.now()/1000); }

function appendMsg(name, text){
  var wc=readU32(1), rec=5+(wc%MAX_REC)*REC;
  writeU32(rec, nowSec());
  var nb=u8(name); if(nb.length>64) nb=nb.slice(0,64); state[rec+4]=nb.length; state.set(nb, rec+5);
  var tb=u8(text); if(tb.length>512) tb=tb.slice(0,512); state[rec+5+64]=tb.length; state.set(tb, rec+5+64+1);
  writeU32(1, wc+1);
  return wc;
}
function readHistory(maxN){
  var wc=readU32(1), total=wc<MAX_REC?wc:MAX_REC, start=wc-total, out=[];
  var from = total-maxN>0 ? total-maxN : 0;
  for(var k=from;k<total;k++){
    var rec=5+((start+k)%MAX_REC)*REC;
    out.push({i:start+k, ts:readU32(rec), name:d8(state,rec+5,state[rec+4]), text:d8(state,rec+5+64+1,state[rec+5+64])});
  }
  return out;
}

function presencePayload(){
  var names=[]; conns.forEach(function(v){ names.push(v.name); });
  return JSON.stringify({t:"presence", n:conns.size, names:names.slice(0,60)});
}
function broadcastPresence(){ pubsub.publish("presence", presencePayload()); }
function broadcastQueue(game){
  var q=queues[game]||[], names=[];
  for(var i=0;i<q.length;i++) names.push(q[i].name);
  pubsub.publish("q:"+game, JSON.stringify({t:"queue", game:game, target:TARGET, count:q.length, names:names.slice(0,40)}));
}
function tryMatch(game){
  var q=queues[game];
  if(!q || q.length<TARGET) return;
  var batch=q.splice(0,TARGET), names=[];
  for(var i=0;i<batch.length;i++) names.push(batch[i].name);
  var payload=JSON.stringify({t:"match", game:game, matchId:Date.now().toString(36)+"-"+Math.floor(Math.random()*0xFFFFFF).toString(36), players:names});
  for(var j=0;j<batch.length;j++){ try{ batch[j].conn.send(payload); }catch(e){} }
  broadcastQueue(game);
  tryMatch(game);
}
function rateAllow(conn, kind){
  var now=Date.now();
  if(kind==="net"){
    var key="n"+conn.net[2], b=rateNet[key];
    if(!b||now-b[1]>60000){ rateNet[key]=[1,now]; return true; }
    if(b[0]<30){ b[0]++; return true; } return false;
  }
  var b=rateConn[conn.id];
  if(!b||now-b[1]>15000){ rateConn[conn.id]=[1,now]; return true; }
  if(b[0]<6){ b[0]++; return true; } return false;
}
function sweep(){
  var now=Date.now(); if(now-lastSweep<60000) return; lastSweep=now;
  for(var k in rateConn){ if(now-rateConn[k][1]>30000) delete rateConn[k]; }
  for(var k2 in rateNet){ if(now-rateNet[k2][1]>120000) delete rateNet[k2]; }
}

self.onmessage = function({conn, data}){
  if(typeof data!=="string") return;
  var m; try{ m=JSON.parse(data); }catch(e){ return; }
  if(!m||!m.t) return;
  sweep();
  if(m.t==="hello"){
    var name=clean(m.name,16)||"Player";
    var had=conns.has(conn.id);
    conns.set(conn.id, {conn:conn, name:name});
    if(!had){ pubsub.publish("lobby", JSON.stringify({t:"sys", text:name+" dropped a coin in the lobby"})); }
    broadcastPresence();
    return;
  }
  if(m.t==="msg"){
    var info=conns.get(conn.id);
    if(!info) return;
    if(!rateAllow(conn)) return;
    var text=clean(m.text,128);
    if(!text || isBanned(text)) return;
    var wc=appendMsg(info.name, text);
    pubsub.publish("lobby", JSON.stringify({t:"msg", i:wc, ts:nowSec(), name:info.name, text:text}));
    return;
  }
};

self.onclose = function({conn}){
  var info=conns.get(conn.id);
  var name=info?info.name:null;
  conns.delete(conn.id);
  delete rateConn[conn.id];
  for(var g in queues){
    var q=queues[g];
    for(var i=q.length-1;i>=0;i--){ if(q[i].conn.id===conn.id){ q.splice(i,1); } }
    broadcastQueue(g);
  }
  if(name){ pubsub.publish("lobby", JSON.stringify({t:"sys", text:name+" stepped away from the cabinet"})); }
  broadcastPresence();
};

self.rpc = {
  history: function({conn}, data){
    return JSON.stringify({t:"hist", msgs:readHistory(100)});
  },
  joinQueue: function({conn}, data){
    var info=conns.get(conn.id);
    if(!info) return JSON.stringify({t:"joinErr", err:"hello first"});
    if(!rateAllow(conn,"net")) return JSON.stringify({t:"joinErr", err:"too fast"});
    var d; try{ d=JSON.parse(data); }catch(e){ return JSON.stringify({t:"joinErr", err:"bad data"}); }
    var game=String(d&&d.game||"").replace(/[^a-z0-9\-]/gi,"").slice(0,40);
    if(!game) return JSON.stringify({t:"joinErr", err:"no game"});
    if(!queues[game]) queues[game]=[];
    var q=queues[game];
    for(var i=0;i<q.length;i++){ if(q[i].conn.id===conn.id) return JSON.stringify({t:"joinErr", err:"already queued"}); }
    if(q.length>=40) return JSON.stringify({t:"joinErr", err:"queue full"});
    try{ conn.subscribe("q:"+game); }catch(e){}
    q.push({conn:conn, name:info.name, at:Date.now()});
    broadcastQueue(game);
    tryMatch(game);
    var still=false, qq=queues[game];
    if(qq){ for(i=0;i<qq.length;i++){ if(qq[i].conn.id===conn.id){ still=true; break; } } }
    var qnames=[], qq2=queues[game];
    if(qq2){ for(i=0;i<qq2.length;i++) qnames.push(qq2[i].name); }
    return JSON.stringify({t: still?"queued":"matched", game:game, target:TARGET, count:still?qnames.length:0, names:qnames.slice(0,40)});
  },
  leaveQueue: function({conn}, data){
    for(var g in queues){
      var q=queues[g];
      for(var i=q.length-1;i>=0;i--){ if(q[i].conn.id===conn.id){ q.splice(i,1); try{ conn.unsubscribe("q:"+g); }catch(e){} broadcastQueue(g); } }
    }
    return JSON.stringify({t:"left", ok:true});
  }
};
</script>

<div class="crt" aria-hidden="true"><div class="crt-bar"></div></div>

<!-- ═══════════ NAV ═══════════ -->
<nav>
  <div class="wrap">
    <a class="nav-brand" href="#top">◤ <b>VIDEO GAME</b> NETWORK ◥</a>
    <div class="nav-links">
      <a href="#about">High Scores</a>
      <a href="#arcade">The Arcade</a>
      <a href="#join">Join</a>
      <a href="#top">Insert Coin</a>
    </div>
  </div>
</nav>

<!-- ═══════════ HERO / STARTUP SCREEN ═══════════ -->
<header class="hero" id="top">
  <canvas id="starfield" aria-hidden="true"></canvas>
  <div class="hero-floor" aria-hidden="true"></div>

  <span class="h-glyph" style="left:7%;top:16%;--gd:-2s">👾</span>
  <span class="h-glyph" style="right:10%;top:20%;--gd:-5s;font-size:2rem">🕹️</span>
  <span class="h-glyph" style="left:14%;bottom:24%;--gd:-7s;font-size:1.8rem">✦</span>
  <span class="h-glyph" style="right:20%;top:56%;--gd:-3s">🟊</span>
  <span class="h-glyph" style="left:22%;top:58%;--gd:-8s;font-size:1.6rem">◤</span>
  <span class="h-glyph" style="right:6%;bottom:30%;--gd:-4s;font-size:1.9rem">🟆</span>

  <div class="hero-content">
    <div class="wordmark"><span class="wm">▶</span> VIDEO GAME NETWORK <span class="wm">◀</span></div>
    <div class="kicker">★ ★ ★ PRESS START ★ ★ ★</div>
    <h1 class="hero-title">
      <span class="t1">VIDEO</span> <span class="t2">GAME</span><br>
      <span class="t3">NETWORK</span>
    </h1>
    <p class="hero-sub">One network. Classic cabinets, rebuilt by hand.<br>Zero quarters required.</p>
    <div class="hero-cta">
      <a class="btn btn-insert" href="#arcade">▶ Insert Coin</a>
      <a class="btn btn-ghost" href="#about">High Scores</a>
    </div>
  </div>

  <div class="press-start" aria-hidden="true">▪ PRESS START ▪</div>
</header>

<!-- ═══════════ MARQUEE ═══════════ -->
<div class="marquee" aria-hidden="true">
  <div class="marquee-track" id="marqueeTrack"></div>
</div>

<!-- ═══════════ ABOUT / STATS ═══════════ -->
<section id="about" style="padding-top:56px">
  <div class="wrap">
    <div class="sec-head">
      <div class="sec-eyebrow">▲ House Rules ▲</div>
      <h2 class="sec-title"><span class="n1">ONE</span> <span class="n2">NETWORK</span> · MANY CABINETS</h2>
      <p class="sec-sub">The Video Game Network is a corner of Perchance where game-makers build and share the classics we grew up on — plus a few original cabinets of our own. Every game below is under construction. Claim a cabinet, bring it to life, and the network plays it forever.</p>
    </div>
    <div class="stats">
      <div class="stat"><b id="statLive">…</b><small>Cabinets Live</small></div>
      <div class="stat"><b id="statSoon">…</b><small>Coming Soon</small></div>
      <div class="stat"><b id="statOnline">…</b><small>Players Online</small></div>
    </div>
  </div>
</section>

<!-- ═══════════ GAMES ═══════════ -->
<section id="arcade" style="padding-top:40px">
  <div class="wrap">
    <div class="sec-head">
      <div class="sec-eyebrow">🕹 The Floor 🕹</div>
      <h2 class="sec-title"><span class="n1">PICK</span> <span class="n2">A</span> <span class="n1">CABINET</span></h2>
      <p class="sec-sub">Our network originals live in the Arcade. The classic tributes are queued up in the Vault. Everything ships as soon as it's built.</p>
    </div>

    <div class="filter-bar">
      <input class="search" id="searchInput" type="text" placeholder="Search cabinets…" aria-label="Search cabinets">
      <div class="chips" id="chips"></div>
    </div>

    <div class="grid" id="grid"></div>
  </div>
</section>

<!-- ═══════════ HOW TO JOIN ═══════════ -->
<section id="join" style="padding-top:20px">
  <div class="wrap">
    <div class="sec-head">
      <div class="sec-eyebrow">▲ Join the Network ▲</div>
      <h2 class="sec-title"><span class="n1">HOW TO</span> <span class="n2">JOIN</span> <span class="n1">THE NETWORK</span></h2>
      <p class="sec-sub">Anyone can build for the Video Game Network. Grab a template, build a classic (or an original), ship it with a <code>vgn-</code> slug — and it earns a cabinet on the Arcade shelf.</p>
    </div>

    <div class="join-steps">
      <div class="jstep"><span class="jnum">1</span><b>Pick a cabinet</b><p>Claim a classic from the Vault, or invent an original nobody's made yet.</p></div>
      <div class="jstep"><span class="jnum">2</span><b>Grab a template</b><p>Fork one of the templates below — the CRT cabinet look is built right in.</p></div>
      <div class="jstep"><span class="jnum">3</span><b>Build your game</b><p>Edit the data, tune the gameplay, swap the art, make it yours.</p></div>
      <div class="jstep"><span class="jnum">4</span><b>Ship it</b><p>Publish it on Perchance with a <code>vgn-</code> name and tell the network — it goes live on the shelf.</p></div>
    </div>

    <div class="grid" id="tplGrid" style="margin-top:46px"></div>
    <p class="more-note">More templates are on the way — puzzlers, shooters, sims, and more. Need one? Say so in the <b>Arcade Lounge</b> (bottom-right).</p>
  </div>
</section>

<!-- ═══════════ LOBBY ═══════════ -->
<style>
  /* ── VGN ARCADE LOUNGE ── */
  .lobby-fab{
    position:fixed;right:22px;bottom:22px;z-index:500;width:60px;height:60px;
    border:1px solid rgba(37,215,253,.5);cursor:pointer;
    background:linear-gradient(150deg,#0f2530,#0a1620 60%,#08121a);
    box-shadow:0 10px 30px rgba(0,0,0,.55), inset 0 0 18px rgba(37,215,253,.12);
    color:var(--neon);font-size:1.7rem;display:flex;align-items:center;justify-content:center;
    clip-path:polygon(10px 0,100% 0,100% calc(100% - 10px),calc(100% - 10px) 100%,0 100%,0 10px);
    transition:transform .3s ease,box-shadow .3s ease;
  }
  .lobby-fab:hover{transform:translateY(-3px);box-shadow:0 16px 40px rgba(0,0,0,.6),0 0 26px rgba(37,215,253,.3)}
  .lobby-fab-badge{
    position:absolute;top:-7px;right:-7px;min-width:22px;height:22px;padding:0 6px;
    background:var(--green);color:#06130e;font-size:.68rem;font-weight:700;
    display:flex;align-items:center;justify-content:center;font-family:var(--term);opacity:.5;
  }
  .lobby-fab-badge.on{opacity:1;background:var(--gold);color:#07090f;box-shadow:0 0 14px rgba(255,211,25,.8)}
  .lobby-backdrop{position:fixed;inset:0;z-index:480;background:rgba(4,6,10,.55);backdrop-filter:blur(3px)}
  .lobby-panel{
    position:fixed;right:18px;bottom:94px;z-index:490;width:min(390px,calc(100vw - 36px));
    height:min(680px,calc(100vh - 124px));display:flex;flex-direction:column;
    background:linear-gradient(180deg,#111a2b,#0c1119);border:1px solid rgba(37,215,253,.28);
    box-shadow:0 30px 80px rgba(0,0,0,.6),0 0 40px rgba(37,215,253,.07);overflow:hidden;
  }
  .lobby-panel[hidden]{display:none}
  .lobby-head{display:flex;align-items:flex-start;justify-content:space-between;padding:16px 18px 12px;border-bottom:1px solid var(--line);background:rgba(37,215,253,.04)}
  .lobby-title{font-size:.68rem;letter-spacing:.08em;color:var(--neon);text-shadow:0 0 12px rgba(37,215,253,.5)}
  .lobby-online{font-family:var(--term);font-size:1rem;color:var(--muted);margin-top:5px;letter-spacing:.04em}
  .lobby-dot{display:inline-block;width:8px;height:8px;background:#5a6476;margin-right:6px;vertical-align:1px}
  .lobby-dot.on{background:var(--green);box-shadow:0 0 8px rgba(61,220,132,.8)}
  .lobby-close{background:none;border:none;color:var(--muted);font-size:1rem;cursor:pointer;padding:4px;font-family:var(--term)}
  .lobby-close:hover{color:var(--pink)}
  .lobby-tabs{display:flex;border-bottom:1px solid var(--line)}
  .lobby-tab{
    flex:1;background:none;border:none;color:var(--muted);padding:12px;
    font-family:var(--pix);letter-spacing:.12em;text-transform:uppercase;font-size:.52rem;
    cursor:pointer;border-bottom:2px solid transparent;transition:all .25s;
  }
  .lobby-tab:hover{color:var(--neon)}
  .lobby-tab.on{color:var(--neon);border-bottom-color:var(--neon);text-shadow:0 0 10px rgba(37,215,253,.5)}
  .lobby-view{padding:14px 16px 16px;display:flex;flex-direction:column;gap:10px;min-height:0;overflow:hidden;flex:1}
  .lobby-view[hidden]{display:none}
  .lobby-names{font-family:var(--term);font-size:.95rem;color:var(--muted);letter-spacing:.03em;min-height:18px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
  .lobby-msgs{flex:1;overflow-y:auto;display:flex;flex-direction:column;gap:8px;min-height:120px;padding-right:4px}
  .lobby-msg{font-family:var(--term);font-size:1.15rem;line-height:1.35}
  .lobby-msg-name{color:var(--gold);font-weight:400;margin-right:7px;text-shadow:0 0 8px rgba(255,211,25,.3)}
  .lobby-msg-time{color:#565f72;font-size:.95rem;margin-left:4px}
  .lobby-msg-text{color:var(--text);word-break:break-word;margin-top:1px}
  .lobby-sys{font-family:var(--term);font-size:1rem;color:#5a6476;font-style:italic;text-align:center}
  .lobby-compose{display:flex;gap:8px;align-items:center;border-top:1px solid var(--line);padding-top:11px}
  .lobby-name{width:90px;background:rgba(13,17,25,.8);border:1px solid var(--line);color:var(--text);padding:9px 10px;font-family:var(--term);font-size:1.05rem;outline:none}
  .lobby-name:focus{border-color:var(--neon)}
  .lobby-msg-input{flex:1;background:rgba(13,17,25,.8);border:1px solid var(--line);color:var(--text);padding:9px 12px;font-family:var(--term);font-size:1.1rem;outline:none}
  .lobby-msg-input:focus{border-color:var(--neon);box-shadow:0 0 0 3px rgba(37,215,253,.12)}
  .lobby-send{width:40px;height:40px;border:none;cursor:pointer;color:#07090f;font-size:.9rem;background:linear-gradient(180deg,#ffe25e,#ffd319 55%,#f0a800);clip-path:polygon(6px 0,100% 0,100% calc(100% - 6px),calc(100% - 6px) 100%,0 100%,0 6px)}
  .lobby-send:hover{filter:brightness(1.08)}
  .lobby-hint{font-family:var(--term);font-size:1.05rem;color:var(--muted);line-height:1.45}
  .lobby-select{width:100%;background:#0a0e16;border:1px solid var(--line);color:var(--text);padding:11px 12px;font-family:var(--term);font-size:1.1rem;outline:none;cursor:pointer}
  .lobby-select:focus{border-color:var(--neon)}
  .lobby-find{
    font-family:var(--pix);letter-spacing:.12em;text-transform:uppercase;font-size:.56rem;cursor:pointer;
    padding:13px 16px;border:none;color:#07090f;
    background:linear-gradient(180deg,#ffe25e,#ffd319 55%,#f0a800);
    box-shadow:0 8px 26px rgba(255,211,25,.3);transition:all .3s;clip-path:polygon(8px 0,100% 0,100% calc(100% - 8px),calc(100% - 8px) 100%,0 100%,0 8px);
  }
  .lobby-find:hover{transform:translateY(-2px);box-shadow:0 12px 32px rgba(255,211,25,.45)}
  .lobby-queue{border-top:1px solid var(--line);padding-top:12px;display:flex;flex-direction:column;gap:7px}
  .lobby-queue[hidden]{display:none}
  .lobby-queue-line{font-family:var(--pix);color:var(--gold);font-size:.58rem;line-height:1.7;letter-spacing:.04em}
  .lobby-queue-names{font-family:var(--term);font-size:1.02rem;color:var(--muted);word-break:break-word}
  .lobby-cancel{
    align-self:flex-start;background:none;border:1px solid rgba(255,90,60,.5);color:#ff9c8a;
    font-family:var(--pix);font-size:.5rem;letter-spacing:.1em;text-transform:uppercase;
    padding:8px 14px;cursor:pointer;transition:all .25s;
  }
  .lobby-cancel:hover{background:rgba(255,90,60,.12);border-color:var(--red)}

  /* match modal */
  .lobby-match-bg{position:fixed;inset:0;z-index:600;background:rgba(4,6,10,.74);backdrop-filter:blur(4px);display:flex;align-items:center;justify-content:center;padding:20px}
  .lobby-match-bg[hidden]{display:none}
  .lobby-match{
    width:min(430px,100%);background:linear-gradient(180deg,#131d30,#0c1119);
    border:1px solid rgba(255,211,25,.5);padding:38px 30px 28px;text-align:center;
    box-shadow:0 40px 110px rgba(0,0,0,.7),0 0 60px rgba(255,211,25,.15);
    animation:lobbyPop .35s ease;clip-path:polygon(14px 0,100% 0,100% calc(100% - 14px),calc(100% - 14px) 100%,0 100%,0 14px);
  }
  @keyframes lobbyPop{from{opacity:0;transform:scale(.92) translateY(10px)}to{opacity:1;transform:none}}
  .lobby-match-burst{font-size:3rem;color:var(--gold);margin-bottom:10px;animation:lobbyBurst 1s ease infinite}
  @keyframes lobbyBurst{0%,100%{transform:rotate(-8deg)}50%{transform:rotate(8deg) scale(1.14)}}
  .lobby-match h3{font-size:.98rem;line-height:1.7;color:var(--gold);text-shadow:0 0 16px rgba(255,211,25,.5)}
  .lobby-match-sub{font-family:var(--term);font-size:1.2rem;color:var(--muted);margin:12px 0 4px}
  .lobby-match-players{font-family:var(--term);font-size:1.15rem;color:var(--text);margin:0 0 22px}
  .lobby-match-btns{display:flex;flex-direction:column;gap:12px;align-items:center}
  .lobby-match-close{display:inline-block;text-decoration:none}
  .lobby-match-later{background:none;border:none;color:var(--muted);font-size:.78rem;margin-top:2px;cursor:pointer;letter-spacing:.08em;font-family:var(--pix);font-size:.5rem}
  .lobby-match-later:hover{color:var(--neon)}
</style>

<button class="lobby-fab" id="lobbyBtn" title="Arcade Lounge" aria-label="Open lobby">
  <span aria-hidden="true">👾</span>
  <span class="lobby-fab-badge" id="onlineBadge">…</span>
</button>

<div class="lobby-backdrop" id="lobbyBackdrop" hidden></div>
<aside class="lobby-panel" id="lobbyPanel" aria-label="Arcade Lounge" hidden>
  <header class="lobby-head">
    <div>
      <h3 class="lobby-title">ARCADE LOUNGE</h3>
      <div class="lobby-online" id="onlineEl"><span class="lobby-dot"></span> connecting…</div>
    </div>
    <button class="lobby-close" id="lobbyCloseBtn" aria-label="Close lobby">✕</button>
  </header>

  <div class="lobby-tabs">
    <button class="lobby-tab on" data-tab="chat">Chat</button>
    <button class="lobby-tab" data-tab="mm">Game Night</button>
  </div>

  <div class="lobby-view" data-view="chat">
    <div class="lobby-names" id="onlineNamesEl"></div>
    <div class="lobby-msgs" id="msgsEl"></div>
    <div class="lobby-compose">
      <input class="lobby-name" id="nameInput" maxlength="16" placeholder="Name" autocomplete="off">
      <input class="lobby-msg-input" id="msgInput" maxlength="128" placeholder="Say something…" autocomplete="off">
      <button class="lobby-send" id="sendBtn" aria-label="Send">➤</button>
    </div>
  </div>

  <div class="lobby-view" data-view="mm" hidden>
    <p class="lobby-hint">Pick a cabinet and hop in the queue. When a second player joins, a game night is locked in — and you're both first in line for opening night.</p>
    <select class="lobby-select" id="matchSel" aria-label="Pick a cabinet"></select>
    <button class="lobby-find" id="findBtn">Find a Game Night</button>
    <div class="lobby-queue" id="queueEl" hidden>
      <div class="lobby-queue-line" id="queueLine"></div>
      <div class="lobby-queue-names" id="queueNames"></div>
      <button class="lobby-cancel" id="cancelBtn">Leave queue</button>
    </div>
  </div>
</aside>

<!-- match-found modal -->
<div class="lobby-match-bg" id="matchBg" hidden>
  <div class="lobby-match">
    <div class="lobby-match-burst" aria-hidden="true">🕹</div>
    <h3>GAME NIGHT LOCKED IN!</h3>
    <p class="lobby-match-sub" id="matchSub"></p>
    <p class="lobby-match-players" id="matchPlayers"></p>
    <div class="lobby-match-btns">
      <a class="btn btn-insert lobby-match-close" id="matchOpenBtn" target="_blank" rel="noopener" hidden>Open the Game ↗</a>
      <button class="btn btn-insert lobby-match-close" id="matchCloseBtn">Back to the Lobby</button>
      <button class="lobby-match-later" id="matchLaterBtn">Stay Here</button>
    </div>
  </div>
</div>

<!-- ═══════════ FOOTER ═══════════ -->
<footer>
  <div class="wrap">
    <div class="ornament">◤ ◥</div>
    <p class="brand">© <span id="year"></span> <b>THE VIDEO GAME NETWORK</b></p>
    <p>Made on <a href="https://perchance.org" target="_blank" rel="noopener">Perchance</a> by the network community.</p>
    <p class="footnote">Maintaining the hub? Add or edit entries in the <code>games</code> list (the shelf) or the <code>templates</code> list (How to Join) in <code>main.pjs</code>.</p>
  </div>
</footer>

<script>
(function(){
  "use strict";

  /* ── starfield hero ── */
  (function(){
    var cv = document.getElementById("starfield");
    if(!cv) return;
    var ctx = cv.getContext("2d");
    var hero = document.querySelector(".hero");
    var W=0, H=0, stars=[], meteors=[];
    function resize(){
      W = cv.width = hero.offsetWidth;
      H = cv.height = hero.offsetHeight;
      stars=[];
      for(var i=0;i<150;i++) stars.push({x:Math.random()*W, y:Math.random()*H, r:Math.random()*1.5+.4, p:Math.random(), sp:Math.random()*.5+.15});
    }
    resize(); window.addEventListener("resize",resize);
    var last=0;
    function frame(t){
      var dt=Math.min(50, t-last||16); last=t;
      ctx.clearRect(0,0,W,H);
      for(var i=0;i<stars.length;i++){
        var s=stars[i];
        s.y+=s.sp*dt/16; if(s.y>H){ s.y=-2; s.x=Math.random()*W; }
        var tw=.5+.5*Math.sin(t/380+s.p*6.283);
        ctx.globalAlpha=.25+tw*.75;
        ctx.fillStyle="#cfe9ff";
        ctx.fillRect(s.x,s.y,s.r,s.r);
      }
      ctx.globalAlpha=1;
      if(Math.random()<dt/1500) meteors.push({x:Math.random()*W*.8+W*.1, y:-10, vx:Math.random()*2+1, vy:Math.random()*3+2, life:0});
      for(var j=meteors.length-1;j>=0;j--){
        var m=meteors[j]; m.x+=m.vx*dt/16; m.y+=m.vy*dt/16; m.life+=dt/16;
        ctx.fillStyle="rgba(150,220,255,.85)"; ctx.fillRect(m.x,m.y,2,2);
        ctx.fillStyle="rgba(150,220,255,.3)"; ctx.fillRect(m.x-m.vx*8, m.y-m.vy*8, m.vx*8, 1.5);
        if(m.life>90) meteors.splice(j,1);
      }
      requestAnimationFrame(frame);
    }
    requestAnimationFrame(frame);
  })();

  /* ── games data from main.pjs ── */
  var PALETTE=["#25d7fd","#ff2bd6","#ffd319","#3ddc84","#ff9d2e","#9b6bff","#ff5a3c","#2ee6c8"];
  function nodeVal(n){return n==null?"":String(typeof n.evaluateItem==="function"?n.evaluateItem:n);}
  var GAMES=[];
  if (window.root && root.games){
    GAMES = root.games.selectAll.map(function(g,i){
      return {
        slug:nodeVal(g.slug), title:nodeVal(g.title), era:nodeVal(g.era)||"Network",
        year:nodeVal(g.year), genre:nodeVal(g.genre), blurb:nodeVal(g.blurb),
        players:nodeVal(g.players), icon:nodeVal(g.icon),
        color:nodeVal(g.color)||PALETTE[i%PALETTE.length],
        image:nodeVal(g.image), comingSoon:nodeVal(g.comingSoon)==="true"
      };
    }).filter(function(g){return g.slug;});
    window.VGN_GAMES = GAMES;
  }

  /* ── marquee ── */
  (function(){
    var parts = GAMES.map(function(g){return (g.icon?g.icon+" ":"")+g.title;}).join(" ");
    var run = parts.split(" ").map(function(w){return w?w:" ";}).join(" ") + " ";
    document.getElementById("marqueeTrack").innerHTML =
      '<span>'+run+' <span class="star">★</span> </span><span>'+run+' <span class="star">★</span> </span>';
  })();

  /* ── stats ── */
  var eras=[];
  GAMES.forEach(function(g){ if(g.era && eras.indexOf(g.era)===-1) eras.push(g.era); });
  var live=GAMES.filter(function(g){return !g.comingSoon;}).length;
  document.getElementById("statLive").textContent = live;
  document.getElementById("statSoon").textContent = GAMES.length - live;
  document.getElementById("statOnline").textContent = "0";

  /* ── filter bar ── */
  var state={q:"", era:"All"};
  var chipEl=document.getElementById("chips");
  function buildChips(){
    var list=["All"].concat(eras);
    chipEl.innerHTML="";
    list.forEach(function(e){
      var b=document.createElement("button");
      b.className="chip"+(e===state.era?" on":"");
      b.textContent=e; b.onclick=function(){ state.era=e; buildChips(); render(); };
      chipEl.appendChild(b);
    });
  }
  document.getElementById("searchInput").addEventListener("input",function(){
    state.q=this.value.toLowerCase().trim(); render();
  });

  /* ── render cabinet grid ── */
  var gridEl=document.getElementById("grid");
  function subhead(title, sub, accent){
    var h=document.createElement("div");
    h.className="subhead gridfade";
    h.style.setProperty("--accent",accent);
    h.innerHTML="<h3><span>"+esc(title)+"</span></h3><small>"+esc(sub)+"</small>";
    gridEl.appendChild(h);
  }
  function card(g){
    var soon=!!g.comingSoon;
    var el=document.createElement(soon?"article":"a");
    el.className="cab gridfade"+(soon?"":" cab-live");
    if(!soon){ el.href="https://perchance.org/"+encodeURIComponent(g.slug); el.target="_blank"; el.rel="noopener"; }
    el.style.setProperty("--accent",g.color);
    var rg=cssRgba(g.color);
    el.style.setProperty("--accentglow",rg(0.26));
    el.style.setProperty("--accentline",rg(0.55));
    el.style.setProperty("--accenttitle",rg(0.45));
    var artImg = g.image
      ? '<img src="'+esc(g.image)+'" alt="'+esc(g.title)+'" loading="lazy">'
      : '<div style="height:100%;background:linear-gradient(150deg,'+g.color+',#0d1220)"></div>';
    var tagSoon = soon ? '<span class="tag tag-soon">★ Coming Soon</span>' : '<span class="tag tag-live">▶ Play Now</span>';
    var eraYear = g.era + (g.year && g.year!=="TBD" ? " · "+g.year : "");
    el.innerHTML =
      '<div class="cab-art">'+artImg+'<div class="crt-lines"></div>'+
        '<div class="cab-tags"><span class="tag">'+esc(eraYear.toUpperCase())+'</span>'+tagSoon+'</div>'+
      '</div>'+
      '<div class="cab-body">'+
        '<div class="cab-title-row"><h3 class="cab-title">'+esc(g.title)+'</h3>'+
          (g.players?'<span class="cab-year">'+esc(g.players)+'</span>':"")+
        '</div>'+
        '<p class="cab-blurb">'+esc(g.blurb)+'</p>'+
        '<div class="cab-foot"><span class="chip">'+esc(g.genre||"Retro")+'</span></div>'+
      '</div>'+
      '<div class="coinbar">'+(soon?'<span class="coin">●</span> <span>COMING SOON</span>':'<span class="coin">●</span> <span class="playnow">INSERT COIN ▶</span>')+'</div>';
    gridEl.appendChild(el);
  }
  function render(){
    var rows=GAMES.filter(function(g){
      var okE = state.era==="All" || g.era===state.era;
      var hay=(g.title+" "+g.genre+" "+g.era+" "+(g.year||"")+" "+g.blurb).toLowerCase();
      var okQ=!state.q || hay.indexOf(state.q)!==-1;
      return okE&&okQ;
    });
    gridEl.innerHTML="";
    if(!rows.length){
      var e=document.createElement("div");
      e.className="empty-state gridfade";
      e.innerHTML='<div class="big">🕹</div>No cabinets match that. Try another search.<br>(Or clean the CRT and start over.)';
      gridEl.appendChild(e);
      return;
    }
    var net=rows.filter(function(g){return g.era==="Network";});
    var classics=rows.filter(function(g){return g.era!=="Network";});
    if(net.length){
      var netLive = net.filter(function(g){return !g.comingSoon;}).length;
      var netSub;
      if(netLive === net.length) netSub = netLive + " network original" + (netLive>1?"s":"") + " — live now";
      else if(netLive > 0) netSub = netLive + " live · " + (net.length - netLive) + " in the workshop";
      else netSub = net.length + " network original" + (net.length>1?"s":"") + " — being built right now";
      subhead("◤ THE ARCADE", netSub, "#25d7fd");
      net.forEach(card);
    }
    if(classics.length){
      subhead("◥ THE VAULT", classics.length+" classic & retro tribute"+(classics.length>1?"s":"")+" — queued for restoration", "#ffd319");
      classics.forEach(card);
    }
  }
  buildChips(); render();

  /* ── templates ("How to join") ── */
  var TPLTS = [];
  if (window.root && root.templates){
    TPLTS = root.templates.selectAll.map(function(t){
      return {
        slug:nodeVal(t.slug), title:nodeVal(t.title), desc:nodeVal(t.desc),
        tag:nodeVal(t.tag), color:nodeVal(t.color)||"#25d7fd",
        icon:nodeVal(t.icon), image:nodeVal(t.image)
      };
    }).filter(function(t){return t.slug;});
    window.VGN_TEMPLATES = TPLTS;
  }
  var tplGrid=document.getElementById("tplGrid");
  if(tplGrid){
    TPLTS.forEach(function(t){
      var el=document.createElement("a");
      el.className="cab gridfade";
      el.href="https://perchance.org/"+encodeURIComponent(t.slug);
      el.target="_blank"; el.rel="noopener";
      el.style.setProperty("--accent",t.color);
      var rg=cssRgba(t.color);
      el.style.setProperty("--accentglow",rg(0.26));
      el.style.setProperty("--accentline",rg(0.55));
      el.style.setProperty("--accenttitle",rg(0.45));
      var art = t.image
        ? '<img src="'+esc(t.image)+'" alt="'+esc(t.title)+'" loading="lazy">'
        : '<div style="height:100%;background:linear-gradient(150deg,'+t.color+',#0d1220)"></div>';
      el.innerHTML =
        '<div class="cab-art">'+art+'<div class="crt-lines"></div>'+
          '<div class="cab-tags"><span class="tag">'+(t.icon?esc(t.icon)+" ":"")+'VGN TEMPLATE</span><span class="tag tag-live">▶ Fork</span></div>'+
        '</div>'+
        '<div class="cab-body">'+
          '<div class="cab-title-row"><h3 class="cab-title">'+esc(t.title)+'</h3></div>'+
          '<p class="cab-blurb">'+esc(t.desc)+'</p>'+
          '<div class="cab-foot"><span class="chip">'+esc(t.tag||"Template")+'</span></div>'+
        '</div>'+
        '<div class="coinbar"><span class="coin">●</span> <span class="playnow">OPEN TEMPLATE ▶</span></div>';
      tplGrid.appendChild(el);
    });
  }

  document.getElementById("year").textContent=new Date().getFullYear();
  function cssRgba(hex){
    var c=String(hex||"#25d7fd").replace("#","");
    if(c.length===3)c=c.split("").map(function(x){return x+x;}).join("");
    var n=parseInt(c,16);
    return function(a){return "rgba("+(n>>16)+","+((n>>8)&255)+","+(n&255)+","+a+")";};
  }
  function esc(s){return String(s==null?"":s).replace(/[&<>"']/g,function(c){return {"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c];});}
})();
</script>

<!-- ═══════════ VGN LOBBY CLIENT — chat + game-night matchmaking UI ═══════════ -->
<script>
(function(){
  "use strict";
  var GAMES = window.VGN_GAMES || [];
  function $(id){ return document.getElementById(id); }
  function esc(s){ return String(s==null?"":s).replace(/[&<>"']/g,function(c){return {"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c];}); }
  function fmtTime(ts){ var d=new Date(ts*1000); return String(d.getHours()).padStart(2,"0")+":"+String(d.getMinutes()).padStart(2,"0"); }

  var socket=null, queued=null, retryDelay=1000, closedByUs=false, lastSeenIdx=-1, myName="";
  try{ myName=localStorage.getItem("vgn_name")||""; }catch(e){}

  var ui = {
    btn:$("lobbyBtn"), badge:$("onlineBadge"), backdrop:$("lobbyBackdrop"), panel:$("lobbyPanel"),
    closeBtn:$("lobbyCloseBtn"), onlineEl:$("onlineEl"), namesEl:$("onlineNamesEl"),
    msgs:$("msgsEl"), input:$("msgInput"), sendBtn:$("sendBtn"), nameInput:$("nameInput"),
    matchSel:$("matchSel"), findBtn:$("findBtn"), queueEl:$("queueEl"),
    queueLine:$("queueLine"), queueNames:$("queueNames"), cancelBtn:$("cancelBtn"),
    matchBg:$("matchBg"), matchSub:$("matchSub"), matchPlayers:$("matchPlayers"),
    matchOpenBtn:$("matchOpenBtn"), matchCloseBtn:$("matchCloseBtn"),
    matchLaterBtn:$("matchLaterBtn"), statOnline:$("statOnline")
  };

  function status(txt, live){
    ui.onlineEl.innerHTML = '<span class="lobby-dot'+(live?" on":"")+'"></span> '+esc(txt);
  }

  /* ── build cabinet dropdown ── */
  GAMES.forEach(function(g){
    var o=document.createElement("option");
    o.value=g.slug; o.textContent=g.title+(g.era==="Network"?" (original)":" (tribute)");
    ui.matchSel.appendChild(o);
  });
  if(!GAMES.length){ ui.findBtn.disabled=true; ui.findBtn.textContent="No cabinets yet"; }

  /* ── socket lifecycle ── */
  function connect(){
    if(!window.root || !root.createServerSocket){ status("lobby offline"); return; }
    status("connecting…");
    var s=root.createServerSocket();
    s.binaryType="arraybuffer";
    s.addEventListener("open",function(){ onOpen(s); });
    s.addEventListener("message",function(ev){ onMessage(ev.data); });
    s.addEventListener("close",function(ev){ onClose(ev); });
    socket=s;
  }
  function onOpen(s){
    retryDelay=1000;
    s.send(JSON.stringify({t:"hello",name:currentName()}));
    if(queued){ try{ s.rpc.joinQueue(JSON.stringify({game:queued.game})); }catch(e){} }
    pullHistory();
  }
  function onMessage(data){
    if(typeof data!=="string") return;
    var m; try{ m=JSON.parse(data); }catch(e){ return; }
    if(!m||!m.t) return;
    if(m.t==="msg"){
      if(m.i!==undefined){ if(m.i<=lastSeenIdx) return; lastSeenIdx=m.i; }
      addMsg(m);
    } else if(m.t==="sys"){ addSys(m.text); }
    else if(m.t==="hist"){
      var max=-1;
      (m.msgs||[]).forEach(function(x){ if(x.i>lastSeenIdx){ addMsg(x); if(x.i>max)max=x.i; } });
      if(max>lastSeenIdx) lastSeenIdx=max;
    } else if(m.t==="presence"){ setPresence(m); }
    else if(m.t==="queue"){ updateQueue(m); }
    else if(m.t==="match"){ showMatch(m); }
  }
  function onClose(ev){
    socket=null; status("reconnecting…");
    if(closedByUs || (ev&&ev.code===4403)){ status("lobby offline"); return; }
    setTimeout(connect,retryDelay);
    retryDelay=Math.min(retryDelay*2,15000);
  }
  async function pullHistory(){
    try{ await socket.opened; var r=await socket.rpc.history(""); onMessage(r); }catch(e){}
  }

  function currentName(){
    var v=ui.nameInput.value.trim().slice(0,16);
    if(v&&v!==myName){ myName=v; try{ localStorage.setItem("vgn_name",myName); }catch(e){} }
    return myName||"Player";
  }

  /* ── chat ── */
  function addMsg(m){
    var row=document.createElement("div"); row.className="lobby-msg";
    row.innerHTML='<span class="lobby-msg-name">'+esc(m.name)+'</span><span class="lobby-msg-time">'+fmtTime(m.ts||0)+'</span><div class="lobby-msg-text">'+esc(m.text)+'</div>';
    ui.msgs.appendChild(row);
    while(ui.msgs.children.length>250) ui.msgs.removeChild(ui.msgs.firstChild);
    ui.msgs.scrollTop=ui.msgs.scrollHeight;
  }
  function addSys(text){
    var row=document.createElement("div"); row.className="lobby-sys"; row.textContent=text;
    ui.msgs.appendChild(row);
    while(ui.msgs.children.length>250) ui.msgs.removeChild(ui.msgs.firstChild);
    ui.msgs.scrollTop=ui.msgs.scrollHeight;
  }
  function sendChat(){
    var text=ui.input.value.trim().slice(0,128);
    if(!text) return;
    if(!socket||socket.readyState!==1){ status("reconnecting…"); return; }
    socket.send(JSON.stringify({t:"msg",text:text}));
    ui.input.value=""; ui.input.focus();
  }

  /* ── presence ── */
  function setPresence(m){
    ui.badge.textContent=m.n||0;
    ui.badge.classList.toggle("on",!!m.n);
    status(m.n?m.n+" online":"nobody online",true);
    if(ui.statOnline) ui.statOnline.textContent=m.n||0;
    var names=(m.names||[]).slice(0,9);
    ui.namesEl.textContent=names.length?("Now here: "+names.join(", ")):"";
  }

  /* ── matchmaking ── */
  async function findMatch(){
    if(queued){ leaveQueue(); return; }
    var slug=ui.matchSel.value, title=ui.matchSel.selectedOptions[0].textContent;
    if(!socket||socket.readyState!==1){ status("reconnecting…"); return; }
    try{
      var r=await socket.rpc.joinQueue(JSON.stringify({game:slug}));
      var m=JSON.parse(r);
      if(m.t==="queued"){ queued={game:slug,title:title}; ui.findBtn.textContent="Waiting…"; ui.queueEl.hidden=false; updateQueue(m); }
      else ui.findBtn.textContent="Find a Game Night";
    }catch(e){}
  }
  function leaveQueue(){
    if(!queued) return;
    var g=queued.game; queued=null;
    ui.findBtn.textContent="Find a Game Night"; ui.queueEl.hidden=true;
    if(socket&&socket.readyState===1){ try{ socket.rpc.leaveQueue(JSON.stringify({game:g})); }catch(e){} }
  }
  function updateQueue(m){
    if(!queued||m.game!==queued.game) return;
    var need=Math.max(0,m.target-m.count);
    ui.queueLine.textContent = need===0 ? "Party's full — locking in…" : (need===1 ? "1 more player needed" : need+" more players needed");
    ui.queueNames.textContent = m.names.length?("In queue: "+m.names.join(", ")):"";
  }

  /* ── match modal ── */
  function showMatch(m){
    var g=GAMES.filter(function(x){return x.slug===m.game;})[0];
    var title=g?g.title:m.game;
    var live = g && !g.comingSoon;
    var others=(m.players||[]).filter(function(p){return p!==currentName();});
    ui.matchSub.textContent=live ? "A table opened up for "+title+"!" : "Game night opened up for "+title+"!";
    ui.matchPlayers.textContent=others.length?("Your crew: "+others.join(", ")):"Solo run — the arcade is quiet tonight.";
    ui.matchOpenBtn.hidden = !live;
    if(live) ui.matchOpenBtn.href = "https://perchance.org/"+encodeURIComponent(m.game);
    ui.matchCloseBtn.classList.toggle("btn-insert", !live);
    ui.matchCloseBtn.classList.toggle("btn-ghost", live);
    ui.matchBg.hidden=false;
    leaveQueue();
  }

  /* ── wiring ── */
  ui.btn.addEventListener("click",function(){
    var open=ui.panel.hidden; ui.panel.hidden=!open; ui.backdrop.hidden=!open;
    if(open){ ui.input.focus(); if(ui.nameInput.value==="") ui.nameInput.focus(); }
  });
  ui.backdrop.addEventListener("click",function(){ ui.panel.hidden=true; ui.backdrop.hidden=true; });
  ui.closeBtn.addEventListener("click",function(){ ui.panel.hidden=true; ui.backdrop.hidden=true; });
  ui.sendBtn.addEventListener("click",sendChat);
  ui.input.addEventListener("keydown",function(e){ if(e.key==="Enter") sendChat(); });
  ui.nameInput.addEventListener("change",function(){
    currentName();
    if(socket&&socket.readyState===1) socket.send(JSON.stringify({t:"hello",name:currentName()}));
  });
  ui.nameInput.value=myName;
  ui.findBtn.addEventListener("click",findMatch);
  ui.cancelBtn.addEventListener("click",leaveQueue);
  ui.matchCloseBtn.addEventListener("click",function(){ ui.matchBg.hidden=true; ui.panel.hidden=true; ui.backdrop.hidden=true; });
  ui.matchLaterBtn.addEventListener("click",function(){ ui.matchBg.hidden=true; });
  ui.matchBg.addEventListener("click",function(e){ if(e.target===ui.matchBg) ui.matchBg.hidden=true; });

  var tabs=document.querySelectorAll(".lobby-tab");
  Array.prototype.forEach.call(tabs,function(t){
    t.addEventListener("click",function(){
      Array.prototype.forEach.call(tabs,function(x){ x.classList.toggle("on",x===t); });
      Array.prototype.forEach.call(document.querySelectorAll(".lobby-view"),function(v){ v.hidden=v.dataset.view!==t.dataset.tab; });
    });
  });

  connect();
})();
</script>

<!-- ═══════════ BACK TO HUB — floating link to rathji's projects ═══════════ -->
<script>
(function(){
  "use strict";
  if(document.getElementById("rathjiBackBtn")) return;

  var style = document.createElement("style");
  style.textContent =
    '#rathjiBackBtn{position:fixed;left:10%;bottom:18px;transform:translateX(-50%);z-index:2147483000;' +
    'display:inline-flex;align-items:center;gap:10px;box-sizing:border-box;' +
    'padding:8px 16px 8px 8px;border-radius:999px;text-decoration:none;color:#fff;' +
    'font-family:system-ui,-apple-system,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;' +
    'background:rgba(13,15,21,.82);-webkit-backdrop-filter:blur(10px);backdrop-filter:blur(10px);' +
    'border:1px solid rgba(255,255,255,.18);' +
    'box-shadow:0 4px 14px rgba(0,0,0,.45),0 1px 3px rgba(0,0,0,.3);' +
    'transition:transform .22s ease,border-color .22s ease,box-shadow .22s ease;' +
    'user-select:none;-webkit-user-select:none;' +
    '}' +
    '#rathjiBackBtn:hover{transform:translateX(-50%) translateY(-2px);' +
    'border-color:rgba(167,139,250,.65);' +
    'box-shadow:0 8px 22px rgba(139,92,246,.28),0 0 18px rgba(139,92,246,.2),0 1px 3px rgba(0,0,0,.3);' +
    '}' +
    '#rathjiBackBtn:active{transform:translateX(-50%) translateY(0)}' +
    '#rathjiBackBtn .rathji-badge{width:28px;height:28px;border-radius:50%;flex:none;' +
    'display:inline-flex;align-items:center;justify-content:center;color:#fff;' +
    'font-size:15px;font-weight:700;line-height:1;' +
    'background:linear-gradient(135deg,#8b5cf6,#d946ef);' +
    '}' +
    '#rathjiBackBtn .rathji-label{font-size:14px;font-weight:600;white-space:nowrap}' +
    '#rathjiBackBtn .rathji-arrow{display:inline-flex;flex:none;opacity:.85}' +
    '@media(max-width:480px){' +
    '#rathjiBackBtn .rathji-label,#rathjiBackBtn .rathji-arrow{display:none}' +
    '#rathjiBackBtn{padding:8px}' +
    '}';
  (document.head || document.documentElement).appendChild(style);

  var a = document.createElement("a");
  a.id = "rathjiBackBtn";
  a.href = "https://perchance.org/rathjis-generators";
  a.target = "_blank";
  a.rel = "noopener";
  a.setAttribute("aria-label", "Back to my projects");
  a.title = "Back to my projects";
  a.innerHTML =
    '<span class="rathji-badge" aria-hidden="true">R</span>' +
    '<span class="rathji-label">My projects</span>' +
    '<svg class="rathji-arrow" width="14" height="14" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">' +
      '<path d="M7 17L17 7M17 7H8M17 7V16" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>' +
    '</svg>';
  document.body.appendChild(a);
})();
</script>
