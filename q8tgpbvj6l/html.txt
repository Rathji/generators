<style>
  :root {
    --bg:#9d00ff; --panel:#141a24; --line:#26313f; --panel2:#1b2331;
    --green:#39ff14; --red:#ff2079; --gold:#ffea00; --cyan:#00fff2; --blue:#2979ff;
    --txt:#e8eef5; --dim:#8fa3b8;
  }
  * { box-sizing:border-box; -webkit-user-select:none; user-select:none; }
  html,body { margin:0; padding:0; height:100%; background:var(--bg); color:var(--txt);
    font-family:system-ui,"Segoe UI",Roboto,sans-serif; overflow:hidden; }
  body { text-align:left; }
  [hidden]{ display:none !important; }
  button { font-family:inherit; }
  a { color:var(--cyan); }

  #gameCtn { position:fixed; inset:0; background:var(--bg); }
  #gameCtn canvas { position:absolute; inset:0; width:100%; height:100%; image-rendering:pixelated; }
  #scanlines { position:fixed; inset:0; z-index:6; pointer-events:none;
    background:repeating-linear-gradient(0deg, rgba(0,0,0,.11) 0 1px, transparent 1px 4px); }

  /* ---------- HUD ---------- */
  #hud { position:fixed; inset:0; pointer-events:none; z-index:10; }
  .hudPanel { position:absolute; background:rgba(8,10,14,.8); border:1px solid var(--line);
    border-radius:10px; padding:8px 12px; backdrop-filter:blur(3px); }
  #timePanel, #scorePanel { background:transparent; border:none; backdrop-filter:none; }
  #statsPanel { top:10px; left:10px; width:230px; }
  .barWrap { height:14px; background:#0c1016; border:1px solid #2c3a4a; border-radius:7px; overflow:hidden; position:relative; }
  .barWrap.thin { height:8px; margin-top:4px; }
  .barWrap .fill { position:absolute; inset:0; width:100%; border-radius:6px; transform-origin:left; }
  #hpBar { background:linear-gradient(180deg,#7bf07f,#3fae44); }
  #shieldBar { background:linear-gradient(180deg,#8fd4ff,#3f7dff); }
  .barLabel { font-size:10px; letter-spacing:1px; color:var(--dim); text-transform:uppercase; margin-bottom:2px; }
  #hpText { position:absolute; right:4px; top:0; font-size:10px; color:#fff; text-shadow:0 1px 2px #000; line-height:14px; font-family:"Courier New",monospace; }
  #statsPanel .statRow { position:relative; }

  #timePanel { top:10px; right:10px; text-align:right; padding:6px 14px; }
  #timeText { font-size:24px; font-weight:800; letter-spacing:2px; font-family:"Courier New",monospace; color:var(--gold); }
  #timePanel .sub { font-size:9px; letter-spacing:3px; color:var(--dim); margin-top:-2px; }
  #xpWrap { position:absolute; top:10px; left:50%; transform:translateX(-50%); width:190px; text-align:center; }
  #xpBarWrap { height:8px; background:#0c1016; border:1px solid #2c3a4a; border-radius:6px; overflow:hidden; position:relative; }
  #xpBar { position:absolute; inset:0; background:linear-gradient(180deg,#a8f27f,#4fae3a); transform-origin:left; width:100%; }
  #xpText { font-size:10px; color:var(--dim); margin-top:3px; letter-spacing:1px; }
  #xpText b { color:var(--green); }

  #scorePanel { top:70px; right:10px; text-align:right; font-family:"Courier New",monospace; }
  #scorePanel .big { font-size:22px; font-weight:800; color:var(--cyan); }
  #scorePanel .row { font-size:12px; color:var(--dim); margin-top:3px; }
  #scorePanel .row b { color:var(--gold); font-size:14px; }

  #ammoPanel { bottom:14px; left:10px; width:210px; }
  #ammoTop { display:flex; align-items:baseline; gap:8px; }
  #ammoText { font-size:30px; font-weight:800; font-family:"Courier New",monospace; color:var(--txt); }
  #gunName { font-size:11px; color:var(--dim); letter-spacing:1px; }
  #reloadBarWrap { height:8px; background:#0c1016; border:1px solid #2c3a4a; border-radius:5px; overflow:hidden; margin-top:4px; position:relative; }
  #reloadBar { position:absolute; inset:0; background:linear-gradient(180deg,#ffd166,#e8a13a); transform-origin:left; width:100%; }
  #reloadHint { font-size:9px; color:var(--dim); margin-top:2px; letter-spacing:1px; }

  #superPanel { position:absolute; bottom:14px; right:10px; text-align:center; pointer-events:auto; }
  #superBtn { position:relative; width:72px; height:72px; border-radius:50%; border:2px solid var(--line);
    background:radial-gradient(circle at 35% 30%, #223044, #10161f); cursor:pointer; color:var(--txt);
    font-weight:800; font-size:9px; letter-spacing:1px; overflow:hidden; }
  #superBtn .meter { position:absolute; bottom:0; left:0; right:0; height:0%;
    background:linear-gradient(180deg,rgba(0,255,242,.6),rgba(41,121,255,.6)); }
  #superBtn .lbl { position:relative; z-index:1; }
  #superUses { position:absolute; top:-6px; right:-6px; background:var(--gold); color:#1a1206;
    border-radius:50%; width:19px; height:19px; line-height:19px; font-size:11px; font-weight:800; border:2px solid #05070a; }
  #superPanel .sub { font-size:8px; color:var(--dim); margin-top:3px; letter-spacing:1px; }


  #perkPanel { position:absolute; left:52px; top:50%; transform:translateY(-50%); width:240px; max-height:60vh;
    overflow-y:auto; pointer-events:auto; }
  #perkPanel h4 { margin:2px 0 6px; font-size:11px; letter-spacing:2px; color:var(--gold); }
  .perkRow { font-size:11px; color:var(--txt); padding:3px 0; border-bottom:1px solid #1d2733; }
  .perkRow .m { color:var(--blue); font-weight:700; }
  #masteryRow { margin-top:6px; font-size:10px; color:var(--dim); }

  #muteBtn { position:fixed; bottom:150px; right:16px; z-index:40; pointer-events:auto;
    background:var(--red); border:1px solid var(--line); border-radius:8px; width:38px; height:38px;
    cursor:pointer; font-size:16px; color:#fff; }

  /* ---------- Damage feedback ---------- */
  #dmgVignette { position:fixed; inset:0; pointer-events:none; z-index:14; opacity:0;
    background:radial-gradient(ellipse at center, transparent 40%, rgba(150,10,10,.42) 72%, rgba(110,0,0,.82) 100%); }
  #vig { position:fixed; inset:0; pointer-events:none; z-index:6; }
  #splatCtn { position:fixed; inset:0; pointer-events:none; z-index:14; overflow:hidden; }
  .splat { position:absolute; width:34px; height:34px; border-radius:50%;
    background:radial-gradient(circle, rgba(190,30,30,.85), rgba(120,0,0,0) 70%);
    transform:translate(-50%,-50%) scale(.2); animation:splatAnim .7s ease-out forwards; }
  @keyframes splatAnim { 0%{ transform:translate(-50%,-50%) scale(.2); opacity:.9; }
    100%{ transform:translate(-50%,-50%) scale(2.4); opacity:0; } }

  /* ---------- Floating text ---------- */
  #floaters { position:fixed; inset:0; pointer-events:none; z-index:20; overflow:hidden; }
  .floater { position:absolute; font-family:"Courier New",monospace; font-size:12px; font-weight:700;
    color:#fff; text-shadow:0 1px 3px #000; animation:floatUp 0.9s ease-out forwards; white-space:nowrap; }
  .floater.crit { color:var(--gold); font-size:16px; }
  .floater.dodge { color:var(--cyan); font-style:italic; }
  .floater.heal { color:var(--green); }
  .floater.fire { color:#ff6b5e; }
  .floater.ammo { color:#9b9b9b; }
  .floater.poison { color:#8cff6b; }
  .floater.banner { left:50%; top:30%; font-size:28px; letter-spacing:3px; animation:bannerIn 2.2s ease-out forwards; }
  @keyframes floatUp { from { opacity:1; transform:translate(-50%,0); } to { opacity:0; transform:translate(-50%,-46px); } }
  @keyframes bannerIn { 0% { opacity:0; transform:translate(-50%,-20px) scale(.7); }
    15% { opacity:1; transform:translate(-50%,0) scale(1); } 70% { opacity:1; } 100% { opacity:0; } }

  /* ---------- Screens ---------- */
  .screen { position:fixed; inset:0; z-index:30; display:flex; flex-direction:column; align-items:center;
    justify-content:center; background:rgba(45,0,70,.92); padding:20px; }
  .screen[hidden]{ display:none !important; }
  .title { font-size:44px; font-weight:900; letter-spacing:6px; margin:0 0 4px; text-shadow:0 0 24px rgba(111,217,255,.35); }
  .title .accent { color:var(--cyan); }
  .subtitle { position:absolute; top:16px; left:0; right:0; color:var(--dim); font-size:11.7px; letter-spacing:2px; margin:0; text-align:center; }
  .stageHead { font-size:24px; font-weight:800; letter-spacing:3px; margin-bottom:4px; color:var(--txt); }
  .stageSub { color:var(--dim); font-size:12px; letter-spacing:1px; margin-bottom:16px; }
  .cards { display:flex; flex-wrap:wrap; gap:10px; justify-content:center; max-width:1120px;
    max-height:62vh; overflow-y:auto; padding:10px 6px 6px; }
  .card { pointer-events:auto; cursor:pointer; background:var(--panel); border:1px solid var(--line);
    border-radius:10px; padding:12px; width:172px; text-align:center; transition:.13s; position:relative; }
  .card:hover { border-color:var(--gold); transform:translateY(-3px); background:var(--panel2); }
  .card .chip { width:44px; height:44px; border-radius:9px; margin:0 auto 8px; display:flex; align-items:center;
    justify-content:center; font-size:22px; font-weight:900; color:#0a0d12; position:relative; overflow:hidden; }
  .picon { position:absolute; inset:2px; background-size:256px 288px; image-rendering:pixelated; }
  .piconSm { display:inline-block; width:18px; height:18px; background-size:144px 162px; image-rendering:pixelated;
    vertical-align:middle; margin-right:6px; border-radius:3px; background-color:rgba(255,255,255,0.05); }
  .card .cname { font-weight:800; letter-spacing:1px; font-size:13px; }
  .card .cdesc { font-size:10.5px; color:var(--dim); margin-top:6px; line-height:1.35; }
  .card .stat { font-size:9.5px; color:var(--cyan); margin-top:6px; letter-spacing:.5px; }
  .card .picked { position:absolute; top:5px; right:8px; color:var(--gold); font-weight:900; font-size:16px; }
  .card.locked { opacity:.32; pointer-events:none; filter:saturate(.3); transform:none; }
  .card.gunCard .cname { color:var(--gold); }
  .card .gunstats { font-family:"Courier New",monospace; font-size:10px; color:var(--dim); margin-top:7px; line-height:1.5; }
  .card .gunstats b { color:var(--txt); }
  .card.boss { border-color:#5c1f2b; }
  .stageBtns { margin-top:16px; display:flex; gap:10px; }
  .selectWrap { display:flex; align-items:center; justify-content:center; gap:14px; width:100%; max-width:1160px; }
  #masteryPanel { flex-shrink:0; width:150px; text-align:left; background:rgba(8,10,14,.85); border:1px solid var(--line);
    border-radius:10px; padding:10px 12px; }
  #masteryPanel h4 { margin:0 0 8px; font-size:11px; letter-spacing:2px; color:var(--gold); }
  .masteryRow { display:flex; justify-content:space-between; font-size:11px; color:var(--dim); padding:2px 0; }
  .masteryRow b { color:var(--gold); font-family:"Courier New",monospace; }
  .masteryRow.king { color:#cf7dff; }
  .masteryRow.king b { color:#cf7dff; }
  #masteryHint { margin-top:8px; font-size:9px; color:var(--cyan); line-height:1.4; letter-spacing:.5px; }
  #selRerollBtn { flex-shrink:0; align-self:flex-start; margin-top:10px; padding:10px 14px; font-size:11px; }
  #selRerollBtn:disabled { opacity:.35; cursor:not-allowed; }
  .dieCard .cname { color:#9ceb5a; }
  .btn { pointer-events:auto; background:#ff00ea; border:1px solid var(--line); color:#1a0033;
    padding:12px 26px; border-radius:9px; cursor:pointer; font-size:14px; font-weight:700; letter-spacing:2px; transition:.13s; }
  .btn:hover { border-color:var(--gold); color:#1a0033; filter:brightness(1.15); }
  .btn.primary { background:linear-gradient(180deg,var(--green),#1acc00); border-color:var(--green); color:#0a1a00; }
  .btn.primary:hover { color:#0a1a00; filter:brightness(1.15); }
  .btn.danger:hover { border-color:var(--red); color:var(--red); background:#2a0018; }

  #menuScreen .menuBtns { display:flex; flex-direction:column; gap:12px; margin-top:8px; }
  #menuScreen #diffSeg { margin-top:8px; }
  #menuScreen #subWrap { gap:8px; }
  #menuScreen .btn { min-width:260px; padding:14px; font-size:15px; }
  .foot { position:absolute; bottom:12px; color:#41516a; font-size:10px; letter-spacing:1px; }

  /* ---------- HUB shop ---------- */
  #menuCreditsLine { margin-top:16px; color:var(--dim); font-size:11px; letter-spacing:2px; }
  #menuCreditsLine b { color:var(--gold); font-size:14px; font-family:"Courier New",monospace; }
  #hubScreen { justify-content:flex-start; }
  .hubHead { display:flex; align-items:center; justify-content:space-between; gap:10px;
    width:100%; max-width:900px; margin:2px 0 10px; }
  .hubHead .btn { padding:8px 18px; font-size:12px; }
  #hubCredits { font-family:"Courier New",monospace; font-size:20px; font-weight:800; color:var(--gold); letter-spacing:1px; }
  #hubHint { width:100%; max-width:900px; color:var(--dim); font-size:11px; letter-spacing:.5px;
    line-height:1.5; margin:10px 0 6px; text-align:center; }
  .hubScroll { flex:1; width:100%; max-width:900px; overflow-y:auto; padding:4px 2px 10px; }
  #hubCards { max-height:none; overflow:visible; }
  .hubCard .price { margin-top:8px; font-size:11px; font-weight:800; letter-spacing:1px; color:var(--gold); font-family:"Courier New",monospace; }
  .hubCard .buyBtn { margin-top:8px; display:block; width:100%; padding:7px 0; border:none; border-radius:7px;
    background:linear-gradient(180deg,var(--gold),#c7b000); color:#1a1206; font-weight:800; font-size:11px; letter-spacing:1px; cursor:pointer; }
  .hubCard .buyBtn:hover { filter:brightness(1.12); }
  .hubCard .buyBtn:disabled { background:#26313f; color:#5a6b80; cursor:not-allowed; filter:none; }
  .hubCard .ownedBadge { margin-top:8px; font-size:10px; letter-spacing:2px; color:var(--green); font-weight:800; }
  .hubCard .pendingBadge { margin-top:8px; font-size:10px; letter-spacing:2px; color:var(--cyan); font-weight:800; }
  .hubCard .cname { font-size:12px; }

  .seg { display:flex; gap:6px; margin-top:16px; background:var(--panel); border:1px solid var(--line);
    border-radius:10px; padding:4px; }
  .segBtn { pointer-events:auto; background:transparent; border:none; color:var(--dim); padding:10px 24px;
    border-radius:7px; cursor:pointer; font-size:12px; font-weight:800; letter-spacing:2px; transition:.12s; }
  .segBtn.active { background:linear-gradient(180deg,#ff9100,#cc7400); color:#1a0d00; }
  .segBtn:not(.active):hover { color:var(--txt); background:#1b2331; }

  #customScreen { justify-content:flex-start; padding:18px 16px 10px; }
  .customScroll { flex:1; width:100%; max-width:1140px; overflow-y:auto; padding:4px 2px 10px; }
  .customGroup { margin-bottom:18px; }
  .groupHead { font-size:13px; letter-spacing:3px; color:var(--gold); margin-bottom:10px; font-weight:800; }
  #customScreen .cards { max-height:none; }
  #customScreen .stageBtns { margin-top:12px; padding-bottom:4px; }

  .summary { max-width:640px; margin-top:14px; background:var(--panel); border:1px solid var(--line);
    border-radius:10px; padding:12px 18px; font-size:12px; color:var(--dim); line-height:1.6; }
  .summary b { color:var(--txt); }
  #endScreen .endTitle { font-size:40px; font-weight:900; letter-spacing:4px; }
  #endScreen .endTitle.win { color:var(--gold); }
  #endScreen .endTitle.lose { color:var(--red); }
  .endStats { display:flex; gap:26px; margin:18px 0; font-family:"Courier New",monospace; text-align:center; }
  .endStats .v { font-size:26px; font-weight:800; }
  .endStats .k { font-size:10px; color:var(--dim); letter-spacing:2px; }
  .endStats .s1 .v{color:var(--cyan);} .endStats .s2 .v{color:var(--gold);}
  .endStats .s3 .v{color:var(--green);} .endStats .s4 .v{color:var(--red);}
  .endStats .s5 .v{color:var(--blue);}

  #bossbarWrap { position:absolute; top:64px; left:50%; transform:translateX(-50%); width:min(420px,60vw); }
  #bossName { text-align:center; font-size:11px; letter-spacing:3px; color:var(--red); margin-bottom:3px; font-weight:800; }
  #bossBar { height:12px; background:#0c1016; border:1px solid #5c1f2b; border-radius:7px; overflow:hidden; }
  #bossFill { height:100%; background:linear-gradient(180deg,#ff7a7a,#c22a2a); width:100%; }

  /* ---------- GitHub backup screen ---------- */
  #ghScreen { justify-content:flex-start; }
  #ghScroll { max-width:660px; }
  .ghCard { background:var(--panel); border:1px solid var(--line); border-radius:10px;
    padding:14px 18px; margin-bottom:14px; }
  .ghCard .groupHead { margin-bottom:8px; }
  .ghField { width:100%; background:#0c1016; border:1px solid var(--line); border-radius:8px;
    color:var(--txt); font-family:inherit; font-size:13px; padding:10px 12px; margin-bottom:10px; }
  .ghField:focus { outline:none; border-color:var(--cyan); }
  .ghField[type="password"] { letter-spacing:.08em; }
  .ghBtns { display:flex; gap:10px; align-items:center; flex-wrap:wrap; }
  .ghBtns .btn { padding:8px 16px; font-size:11px; }
  .ghNote { font-size:11px; color:var(--dim); line-height:1.6; margin:8px 0 4px; }
  .ghNote code { background:#0c1016; border:1px solid var(--line); border-radius:4px; padding:0 5px; color:var(--cyan); }
  .ghStatus { font-size:11px; color:var(--green); }
  .ghStatus.err { color:var(--red); }
  .ghOut { margin-top:10px; font-size:11.5px; color:var(--cyan); line-height:1.7; }
  .ghOut.err { color:var(--red); }
  .ghOut a { word-break:break-all; }
  #ghRestoreOut textarea { width:100%; height:110px; background:#0c1016; border:1px solid var(--line);
    border-radius:8px; color:var(--dim); font-family:ui-monospace,Consolas,monospace; font-size:11px;
    padding:8px 10px; margin-top:6px; resize:vertical; }

  @media (max-width:700px){
    .title{font-size:30px;} .card{width:142px;}
    #timeText{font-size:20px;} #scorePanel .big{font-size:17px;}
    #superBtn{width:56px;height:56px;}
  }
  /* ---------- Mobile (pointer:coarse) — joysticks, super button, minimal HUD ---------- */
  .joy { display:none; }
  #touchAmmo { display:none; }
  #touchSuperBtn { display:none; }
  #rotateOv { position:fixed; inset:0; z-index:80; background:rgba(3,5,8,.97);
    display:flex; align-items:center; justify-content:center; flex-direction:column; gap:16px; text-align:center; }
  #rotateOv .icon { font-size:58px; animation:rotHint 1.5s ease-in-out infinite; }
  #rotateOv .msg { font-size:15px; font-weight:800; letter-spacing:2px; color:var(--txt); line-height:1.8; }
  #rotateOv .msg span { display:block; font-size:11px; color:var(--dim); font-weight:400; margin-top:4px; }
  @keyframes rotHint { 0%,100% { transform:rotate(0deg); } 45% { transform:rotate(90deg); } }

  @media (pointer:coarse){
    .joy { display:block; position:fixed; width:132px; height:132px; border-radius:50%;
      pointer-events:none; z-index:11; opacity:.32; transform:scale(.78);
      transition:left .18s ease, top .18s ease, opacity .18s ease, transform .18s ease; }
    .joy.dragging { transition:none; opacity:1; transform:scale(1); }
    .joy .jbase { position:absolute; inset:0; border-radius:50%;
      border:3px solid rgba(255,255,255,.25);
      background:radial-gradient(circle at 50% 40%, rgba(255,255,255,.09), rgba(255,255,255,.02)); }
    .joy .jknob { position:absolute; left:50%; top:50%; width:52px; height:52px; margin:-26px 0 0 -26px;
      border-radius:50%; border:2px solid rgba(255,255,255,.45);
      background:radial-gradient(circle at 35% 30%, rgba(255,255,255,.55), rgba(255,255,255,.16));
      box-shadow:0 2px 10px rgba(0,0,0,.55); }
    #joyL { left:16px; top:calc(100% - 148px); }
    #joyR { left:calc(100% - 148px); top:calc(100% - 148px); }

    #touchAmmo { display:block; position:fixed; right:16px; top:calc(100% - 148px - 40px); z-index:11;
      pointer-events:none; text-align:right; font-family:"Courier New",monospace; }
    #touchAmmo #touchAmmoTop { font-size:16px; font-weight:800; line-height:1; }
    #touchAmmo #touchAmmoTop b { color:var(--cyan); }
    #touchAmmo #tGunName { font-size:8px; color:var(--dim); letter-spacing:1px; margin-left:6px; }
    #touchReloadWrap { height:5px; width:70px; margin:3px 0 0 auto; background:#0c1016; border:1px solid #2c3a4a;
      border-radius:3px; overflow:hidden; }
    #touchReloadBar { height:100%; width:0%; background:linear-gradient(180deg,#ffd166,#e8a13a); }

    #touchSuperBtn { display:block; position:fixed; left:50%; bottom:16px; transform:translateX(-50%);
      z-index:12; pointer-events:auto; width:46px; height:46px; border-radius:50%;
      border:2px solid var(--line); color:var(--gold);
      background:radial-gradient(circle at 35% 30%, #3a3a55, #161b28); cursor:pointer;
      font-weight:800; font-size:7px; letter-spacing:1px; overflow:hidden; }
    #touchSuperBtn .meter { position:absolute; bottom:0; left:0; right:0; height:0%;
      background:linear-gradient(180deg,rgba(0,255,242,.7),rgba(41,121,255,.7)); }
    #touchSuperBtn .lbl { position:relative; z-index:1; text-shadow:0 1px 3px #000; }

    /* compact, edge-hugging HUD so the arena edges stay visible */
    #statsPanel { top:6px; left:6px; width:auto; padding:4px 9px; border-radius:8px; }
    #statsPanel .barWrap { height:10px; }
    #statsPanel .barWrap.thin { display:none; }
    #statusLabel { display:none; }
    #hpText { font-size:9px; line-height:10px; }
    #timePanel { top:6px; right:44px; padding:3px 10px; border-radius:8px; }
    #timeText { font-size:17px; }
    #timePanel .sub { display:none; }
    #xpWrap { top:8px; width:min(150px,38vw); }
    #xpBarWrap { height:6px; }
    #xpText { display:none; }
    #scorePanel { top:46px; right:6px; padding:3px 10px; border-radius:8px; }
    #scorePanel .big { font-size:14px; }
    #scorePanel .row { font-size:9px; margin-top:1px; }
    #scorePanel .row b { font-size:10px; }
    #ammoPanel { display:none; }
    #superPanel { display:none; }
    #bossbarWrap { top:48px; width:min(320px,52vw); }
    #perkPanel { left:auto; right:6px; top:auto; bottom:108px; width:168px; max-height:40vh; transform:none; }
    #muteBtn { top:8px; right:6px; bottom:auto; width:30px; height:30px; font-size:13px; z-index:41; }
    #scanlines { opacity:.45; }

    /* screens */
    .title { font-size:26px; letter-spacing:3px; }
    .screen { padding:10px; }
    .cards { gap:6px; }
    #selectCards .card { width:112px; padding:7px; }
    #selectCards .card .chip { width:34px; height:34px; font-size:17px; border-radius:7px; margin-bottom:6px; }
    #selectCards .card .cname { font-size:11px; }
    #selectCards .card .cdesc { font-size:9.5px; line-height:1.3; margin-top:5px; }
    #selectCards .card .stat { font-size:8px; margin-top:4px; }
    .card { width:118px; padding:8px; }
    .card .chip { width:36px; height:36px; font-size:18px; }
    .card .cname { font-size:11px; }
    .card .cdesc { font-size:9px; line-height:1.25; }
    .card .stat { font-size:8px; }
    .card .gunstats { font-size:8px; }
    .stageHead { font-size:18px; }
    .stageSub { font-size:10px; margin-bottom:10px; }
    .subtitle { top:10px; font-size:9.5px; letter-spacing:1px; }
    .btn { padding:10px 16px; font-size:12px; }
    .segBtn { padding:8px 14px; font-size:11px; }
    .summary { font-size:11px; padding:8px 12px; margin-top:10px; }
    .endStats { gap:16px; margin:12px 0; }
    .endStats .v { font-size:20px; }
    #menuScreen { gap:10px; }
    #menuScreen .menuBtns .btn { min-width:0; padding:12px 20px; font-size:14px; }
    #menuScreen #howWrap { display:none; }
    #diffSeg { order:1; }
    #startWrap { order:2; }
    #menuScreen #subWrap { order:3; flex-direction:row; flex-wrap:wrap; justify-content:center; gap:8px; }
    #menuScreen #subWrap .btn { padding:8px 12px; font-size:10px; letter-spacing:1px; min-width:0; }
    #masteryPanel { display:none; }
    .selectWrap { gap:8px; }
    #selRerollBtn { margin-top:8px; padding:8px 10px; font-size:10px; }
    .foot { display:none; }
    .hubHead, .hubScroll { max-width:100%; }
  }
</style>

<div id="gameCtn"></div>
<div id="scanlines"></div>

<div id="hud" hidden>
  <div class="hudPanel" id="statsPanel">
    <div class="statRow">
      <div class="barWrap"><div class="fill" id="hpBar"></div><span id="hpText">60/60</span></div>
    </div>
    <div class="statRow">
      <div class="barWrap thin"><div class="fill" id="shieldBar"></div></div>
    </div>
    <div class="barLabel" id="statusLabel">&nbsp;</div>
  </div>

  <div class="hudPanel" id="timePanel">
    <div id="timeText">500</div>
    <div class="sub">SURVIVE</div>
  </div>

  <div id="bossbarWrap" hidden><div id="bossName"></div><div id="bossBar"><div id="bossFill"></div></div></div>

  <div id="xpWrap">
    <div id="xpBarWrap"><div id="xpBar"></div></div>
    <div id="xpText">LEVEL <b id="lvlText">1</b> &nbsp;·&nbsp; EXP <span id="expText">0/10</span></div>
  </div>

  <div class="hudPanel" id="scorePanel">
    <div class="big" id="scoreText">0</div>
    <div class="row">G <b id="goldText">0</b></div>
    <div class="row">K <b id="killsText">0</b></div>
  </div>

  <div class="hudPanel" id="ammoPanel">
    <div id="ammoTop"><div id="ammoText">12</div><div id="gunName">BLASTER-SG12</div></div>
    <div id="reloadBarWrap"><div id="reloadBar" style="width:0%"></div></div>
    <div id="reloadHint">CLICK / R TO RELOAD</div>
  </div>

  <div id="superPanel">
    <button id="superBtn"><div class="meter" id="superMeter"></div><div class="lbl">SUPER<br>(E)</div></button>
    <div id="superUses">3</div>
    <div class="sub" id="superSub">READY IN 12s</div>
  </div>

  <button id="touchSuperBtn"><div class="meter" id="touchSuperMeter"></div><div class="lbl">SUPER</div></button>

  <div id="touchAmmo"><div id="touchAmmoTop"><b id="tAmmoText">12</b><span id="tGunName"></span></div><div id="touchReloadWrap"><div id="touchReloadBar"></div></div></div>

  <div class="hudPanel" id="perkPanel" hidden>
    <h4>UPGRADES</h4>
    <div id="perkList"></div>
    <div id="masteryRow"></div>
  </div>
</div>

<div id="vig"></div>
<div id="dmgVignette"></div>
<div id="splatCtn"></div>
<div id="floaters"></div>
<button id="muteBtn">🔊</button>
<div id="joyL" class="joy"><div class="jbase"></div><div class="jknob"></div></div>
<div id="joyR" class="joy"><div class="jbase"></div><div class="jknob"></div></div>

<div id="rotateOv" hidden>
  <div class="icon">📱</div>
  <div class="msg">ROTATE YOUR DEVICE<span>landscape gives you the best view of the arena</span></div>
  <button class="btn" id="rotateGoBtn">GO FULLSCREEN</button>
</div>

<div id="menuScreen" class="screen">
  <div class="title" style="color:var(--red); letter-spacing:4px; font-size:40px; text-shadow:0 0 26px rgba(255,90,90,.45);">⚠ NOT MY GENERATOR ⚠</div>
  <div class="subtitle" style="top:34px; max-width:720px; color:var(--dim); font-size:12.5px; line-height:1.7; margin:0 auto;">
    <b style="color:var(--gold);">DISCLAIMER:</b> this page is <b style="color:var(--txt);">not my work</b> — I copied it from its original creator purely to test a Perchance plugin.
    All credit goes to the original author. See <a href="https://perchance.org/rathjis-plugin-tutorials" target="_blank" rel="noopener">Rathji's Plugin Tutorials</a>.
    (Original game: <a href="https://gd.games/penusbmic/bullet-bunny" target="_blank" rel="noopener">Bullet Bunny</a> — survive 500 seconds.)
  </div>
  <div class="menuBtns" id="howWrap">
    <button class="btn" id="howBtn">HOW TO PLAY</button>
  </div>
  <div class="seg" id="diffSeg">
    <button class="segBtn active" data-d="Normal">NORMAL</button>
    <button class="segBtn" data-d="Hard">HARD</button>
    <button class="segBtn" data-d="Insane">INSANE</button>
  </div>
  <div class="menuBtns" id="startWrap">
    <a class="btn" id="origBtn" href="https://gd.games/penusbmic/bullet-bunny" target="_blank" rel="noopener" style="background:linear-gradient(180deg,#00fff2,#2979ff); border-color:#00fff2; color:#06202e; text-decoration:none;">▶ PLAY THE ORIGINAL: BULLET BUNNY</a>
    <button class="btn primary" id="startBtn">START</button>
  </div>
  <div class="menuBtns" id="subWrap">
    <button class="btn" id="customBtn">CUSTOMIZE CHARACTER</button>
    <button class="btn" id="hubBtn">HUB SHOP</button>
    <button class="btn" id="ghBtn">🐙 GITHUB BACKUP</button>
    <button class="btn" id="debugBtn" hidden>DEBUG SOUNDS</button>
  </div>
  <div id="menuCreditsLine">BANK <b id="menuCredits">0</b> CREDITS</div>
  <div class="foot">MOVEMENT WASD · AIM MOUSE · FIRE HOLD LMB · RELOAD CLICK / R · SUPER E</div>
</div>

<div id="selectScreen" class="screen" hidden>
  <div class="stageHead" id="stageHead"></div>
  <div class="stageSub" id="stageSub"></div>
  <div class="selectWrap">
    <div id="masteryPanel" hidden>
      <h4>MASTERY</h4>
      <div id="masteryList"></div>
      <div id="masteryHint"></div>
    </div>
    <div class="cards" id="selectCards"></div>
  </div>
  <div class="stageBtns">
    <button class="btn danger" id="selBackBtn" hidden>BACK</button>
    <button class="btn primary" id="selNextBtn" hidden>NEXT</button>
  </div>
</div>

<div id="customScreen" class="screen" hidden>
  <div class="stageHead">CUSTOMIZE CHARACTER</div>
  <div class="stageSub">Tap a bunny, a weapon and a gem — then deploy</div>
  <div class="customScroll" id="customScroll">
    <div class="customGroup">
      <div class="groupHead">1 · BUNNY</div>
      <div class="cards" id="ccChar"></div>
    </div>
    <div class="customGroup">
      <div class="groupHead">2 · WEAPON</div>
      <div class="cards" id="ccGun"></div>
    </div>
    <div class="customGroup">
      <div class="groupHead">3 · GEM</div>
      <div class="cards" id="ccGem"></div>
    </div>
    <div class="customGroup">
      <div class="groupHead">4 · MUSIC</div>
      <div class="cards" id="ccMusic"></div>
    </div>
  </div>
  <div class="stageBtns">
    <button class="btn" id="customBackBtn">BACK</button>
    <button class="btn primary" id="customGoBtn">DEPLOY</button>
  </div>
</div>

<div id="hubScreen" class="screen" hidden>
  <div class="stageHead">HUB</div>
  <div class="stageSub">Spend run earnings on permanent unlocks</div>
  <div class="hubHead">
    <button class="btn" id="hubBackBtn">BACK</button>
    <div>CREDITS <b id="hubCredits">0</b></div>
  </div>
  <div class="seg" id="hubTabs">
    <button class="segBtn active" data-t="gems">GEMS</button>
    <button class="segBtn" data-t="heroes">HEROES</button>
    <button class="segBtn" data-t="guns">GUNS</button>
    <button class="segBtn" data-t="vending">VENDING</button>
  </div>
  <div id="hubHint"></div>
  <div class="hubScroll">
    <div class="cards" id="hubCards"></div>
  </div>
</div>

<div id="ghScreen" class="screen" hidden>
  <div class="stageHead">🐙 GITHUB BACKUP</div>
  <div class="stageSub">Powered by <a href="https://perchance.org/github-data-plugin" target="_blank" rel="noopener">github-data-plugin</a> — your token never leaves this browser</div>
  <div class="hubHead">
    <button class="btn" id="ghBackBtn">BACK</button>
  </div>
  <div class="hubScroll" id="ghScroll">
    <div class="ghCard">
      <div class="groupHead">1 · CONNECT</div>
      <input id="ghRepoInput" class="ghField" value="Rathji/generators" placeholder="owner/repo — e.g. yourname/backups">
      <input id="ghTokenInput" class="ghField" type="password" placeholder="GitHub token (fine-grained, Contents: read+write, repo-scoped)">
      <div class="ghBtns">
        <button class="btn" id="ghSaveTokenBtn">SAVE TOKEN</button>
        <button class="btn danger" id="ghClearTokenBtn">CLEAR</button>
        <span id="ghTokenStatus" class="ghStatus"></span>
      </div>
      <p class="ghNote">🔑 Token is stored only in this browser (localStorage) and sent only to <code>api.github.com</code>. Never paste it anywhere public — this generator's source is visible to everyone. Create one at <a href="https://github.com/settings/personal-access-tokens" target="_blank" rel="noopener">github.com/settings/personal-access-tokens</a>.</p>
    </div>
    <div class="ghCard">
      <div class="groupHead">2 · BACKUP → GITHUB</div>
      <p class="ghNote">Pushes this generator's last-saved Lists/HTML panels to <code>&lt;generator-name&gt;/lists.txt</code> + <code>&lt;generator-name&gt;/html.txt</code> in the repo, as a single commit.</p>
      <div class="ghBtns">
        <button class="btn primary" id="ghBackupBtn">BACKUP TO GITHUB →</button>
      </div>
      <div id="ghBackupOut" class="ghOut"></div>
    </div>
    <div class="ghCard">
      <div class="groupHead">3 · RESTORE ← GITHUB</div>
      <p class="ghNote">Fetches <code>&lt;generator-name&gt;/lists.txt</code> + <code>&lt;generator-name&gt;/html.txt</code> from the repo so you can review or download them.</p>
      <div class="ghBtns">
        <button class="btn" id="ghFetchBtn">FETCH BACKUP</button>
        <button class="btn" id="ghDlMainBtn" disabled>⬇ lists.txt</button>
        <button class="btn" id="ghDlHtmlBtn" disabled>⬇ html.txt</button>
      </div>
      <div id="ghRestoreOut" class="ghOut"></div>
    </div>
    <div id="ghStatus" class="ghOut"></div>
  </div>
</div>

<div id="debugScreen" class="screen" hidden>
  <div class="stageHead">DEBUG</div>
  <div class="stageSub">Sound test — buttons play each chiptune effect</div>
  <div class="hubHead">
    <button class="btn" id="debugBackBtn">BACK</button>
  </div>
  <div class="hubScroll">
    <div class="cards" id="debugCards"></div>
  </div>
</div>

<div id="pauseScreen" class="screen" hidden>
  <div class="stageHead">PAUSED</div>
  <div class="stageBtns">
    <button class="btn primary" id="resumeBtn">RESUME</button>
    <button class="btn danger" id="quitBtn">QUIT TO MENU</button>
  </div>
</div>

<div id="endScreen" class="screen" hidden>
  <div class="endTitle" id="endTitle"></div>
  <div class="subtitle" id="endSub"></div>
  <div class="endStats">
    <div class="s1"><div class="v" id="endScore">0</div><div class="k">SCORE</div></div>
    <div class="s2"><div class="v" id="endGold">0</div><div class="k">GOLD</div></div>
    <div class="s3"><div class="v" id="endKills">0</div><div class="k">KILLS</div></div>
    <div class="s4"><div class="v" id="endTime">0</div><div class="k">SECONDS</div></div>
    <div class="s5"><div class="v" id="endLevel">0</div><div class="k">LEVEL</div></div>
  </div>
  <div class="stageBtns">
    <button class="btn primary" id="retryBtn">TRY AGAIN</button>
    <button class="btn" id="menuBtn">MAIN MENU</button>
  </div>
</div>

<script type="module" src="src/main.js"></script>
